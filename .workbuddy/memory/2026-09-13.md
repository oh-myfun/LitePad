# 2026-09-13 工作记录

## 重大事故：会话内 `.git` 丢失（必须记牢）

- 排查 B23 时执行 `git stash -u`（想对照干净树基线），命令报
  `fatal: Unable to create '.git/index.lock': No such file or directory`，
  **之后 `/e/Project/LitePad/.git` 整个目录消失**（find 全盘无对象、无回收站、无远程）。
- node_modules/dist/target 等无关；**所有源码/测试文件完好**，仅 git 元数据丢失。
- 现场无远程（package.json 无 repository，无 `.git` 残留），历史（M0–M22 全部提交）不可恢复。
- 恢复动作：`git init -q` → `git symbolic-ref HEAD refs/heads/main` → 按 `.gitignore`
  （已含 node_modules/dist/target/generated-images/.vite/src-tauri/gen 等）只 `git add`
  src tests src-tauri index.html 配置 md .workbuddy，**分两个 Conventional Commit**：
  `c47c542 chore(repo): 重建 git 仓库（…历史不可恢复）` 基线 + `e1ecc90 fix(preview): B23 …`。
- 教训：会话沙箱内**切勿随意 `git stash -u / rm` 等会触碰 `.git` 的重操作**；
  对照基线请改用 `git stash` 之外的方式（如另存 diff 文件）。用户真实机器上 .git 是否也丢未知，
  交付时务必提示用户核查其本地/远程仓库。

## B23 修复（大纲跳转不准 + 大纲不随文档切换）

用户原话：markdown 预览下随大纲跳转位置仍不准；分屏打开 文本+md 并切换时，大纲始终显示 md 的大纲。

### (a) 跳转位置不准 根因与修复
- **根因1**：`handleUpdate` 里 `if (isMdTab(tab)) scheduleMdRender(panel.panelId)`
  对每个 view update（含仅改选区的 dispatch，如大纲跳转）都排程 120ms 重渲染；
  `setBlocks` 的 `replaceChildren` 把 `scrollTop` 清零 → 跳转落点被冲掉（时序竞态，故「有时候不准」）。
  - 修复：重渲染改由「文本真的变了」门控 `if (textChanged && isMdTab(tab))`。
- **根因2**：`setBlocks` 重渲染不保留滚动位置（replaceChildren 清零），且 `pendingSyncLine`
  永不失效 → 之后任何重排/异步增强都会把视图拽回历史跳转点。
  - 修复：重渲染前记 `prevScroll`，插完若无 pending 则原地恢复并把 `programmaticTop` 设成该值；
    若 `pendingSyncLine` 存在则立即 `applySyncToLine`；**内容真的变了则把 `pendingSyncLine` 置 null**。
- **根因3**：程序滚动的 scroll 回执在 120ms 同步锁过期后才到达时，被 `onPreviewScroll` 误判成
  用户滚动 → 清掉 pendingSyncLine。
  - 修复：`onPreviewScroll` 先比对 `programmaticTop`，相符则认领为自家滚动直接 return；
    锁过期回调里一并清空 `programmaticTop`。
- **增强**：`applyPanelMode` 在隐藏编辑器**之前**取 `topVisibleLineOf(view)`，切预览/切标签时
  调 `preview.syncToLine(anchorLine)` 把预览对齐到同一区域（新方法 `topVisibleLineOf` 抽出复用）。

### (b) 大纲不随活动文档切换 根因与修复
- **根因**：`onActivatePanel`（splitview 的 `.layout-panel` mousedown 触发）只改 `activePanelId`，
  未刷新大纲；而 `switchTab` 里的 `updateTocDrawer()` 被 `if (panelId === activePanelId)` 挡住——
  分屏下点**另一块面板**（不是点标签）走 onActivatePanel 路径，不触发 TOC 刷新。
- **修复**：`onActivatePanel` 内 `activePanelId = panelId` 之后补 `updateTocDrawer()`。
  （其他 `activePanelId` 变更点都已 `refreshAll()` → 含 `updateTocDrawer`，无需改。）

### 测试
- 新增 `tests/toc-follows-panel.test.ts`（真实 bootstrap：左 md / 右纯文本分屏，切换面板断言大纲切换/清空）。
- `tests/preview-sync.test.ts` 扩展：重渲染保位、内容不变立即定位、内容变化作废 pending、
  程序滚动回执不误清 pending；新增静态断言 `scheduleMdRender 必须 textChanged 门控`。
- jsdom 环境坑：jsdom 24 未实现 `Range.getClientRects/getBoundingClientRect`，CM6 在 mousedown
  里调用会抛 `getClientRects is not a function`，导致 `smoke.bootstrap.test.ts`（5 例）与
  `view-switch-noedit.test.ts` 的未捕获错误。在两测试 beforeAll 补 Range 桩（模拟 Chromium 行为）。
- 全量 **111/111 通过（13 文件）**；`tsc -b` 通过；`vite build` 通过。

## 验证待办（桌面环境，沙箱无法 UI 冒烟）
- B23a 预览态点大纲跳转落点是否真准；切预览/切标签时预览是否对齐编辑区
- B23b 分屏 文本+md 互切、点另一面板时大纲是否立即跟随
- 发布版（tauri build release）是否已嵌入 B23 前端

## B24 文件拖入增强（3cf9366）

用户要求：拖文件到窗口时显示分屏落点预览、可拖入不同面板/分屏区域打开；拖 Markdown 时弹菜单选「插入文件路径 / 打开文档」。

- **落点预览**：`onDragDropEvent` 的 `enter/over` 复用标签拖拽的 `.split-preview` 层
  （`showFileDropPreview` → `panelAt`+`zoneOf`），`leave` 清除（`clearAllDropPreviews`）。
- **分区打开**：`doOpen` 加第三参 `targetPanelId` 并返回 tabId；drop 中央=落进该面板，
  边缘=`splitPanelWithTab(target, dir, tabId, newFirst)` 旁分屏。
- **md 选择菜单**：单个 Markdown 落地弹 `showFileDropChoice`（打开文档 / 插入文件路径）。
  **关键约束：原生 OS 拖拽期间鼠标被系统捕获，页面控件收不到点击 → 菜单只能在 drop 后弹出**，
  不能在 over 时机弹。`showPopupMenu` 因此支持 `at?: {x,y}` 无锚点坐标弹出（menu.ts 改造）。
- 「插入文件路径」= 写入活动编辑器光标处（无编辑器回落为打开）。
- **坐标换算**：Tauri 拖放事件 position 是物理像素，DOM 是逻辑像素 → 除以 `devicePixelRatio`
  （`dropPosOf`）。若用户反馈落点有偏移，优先怀疑原点是窗口含标题栏（需桌面实测校正）。
- splitview 导出 `panelAt / zoneOf / clearAllDropPreviews`（原来私有）。
- 测试：`tests/filedrop.test.ts`（md 识别、needsChoice、选择菜单 DOM、接线静态断言）；
  smoke「拖入打开」用例改为「先弹菜单 → 点打开 → 内容出现」。
- 教训：smoke 旧用例 fireDragDrop(["e.md"]) 期待立即打开，新交互下必须点菜单项——
  **改拖入交互时记得同步改该用例**。

## B25 图标四角圆角统一（e3d6487）

用户报告：图标下边缘接近直角，要和上边缘一样圆，且应用到 exe/安装包。

- 实测源图 `generated-images/icon_final.png`（1024²）：上边缘圆角 ~100px、下边缘所有列
  bottom=1023（完全直角）。
- **修法：alpha 通道与垂直镜像后的 alpha 逐像素取 min（`ImageChops.darker(alpha, mirror)`）
  → 下边缘获得与上边缘完全一致的圆角，无需猜半径**；校验 128px 图 bottom == 127 - top 逐列成立。
- 重写 `scripts/gen_icons.py`：优先 Pillow + 源图（四角统一 + 多尺寸 ICO 16–256），
  缺源图/无 Pillow 时退回内置纯标准库占位图。venv python 在
  `~/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（有 pillow 12.3.0）；
  注意 `Image` 常量要在使用它的函数里 import（跨函数会 NameError）。
- exe/NSIS 图标来自 tauri.conf.json `bundle.icon`（icon.ico）→ 重跑 tauri build 生效。
- 对比图：generated-images/icon_compare.png（左旧右新）。

## B26 修复：开关文档时分割条跳位

用户报告：打开/关闭文档时面板间分割条位置会变。

- **根因**：`splitview.build()` 传给 `onRatioChange` 的 `path` 是**分割节点自身**的树路径
  （根分割 = `[]`），而 main.ts 旧本地 `updateRatio` 按「寻址子节点」解释（要求 path 末位是 0/1）
  → 错位一层：根分割的 ratio 永远写不进树、嵌套分割写到父节点。平时拖拽只改内联样式看不出，
  任何 `rebuildLayout()`（开/关文档）都按旧 ratio 重排 → 跳位。
- **修复**：删除 main.ts 本地 `updateRatio`，改从 `./shell/layout` 导入新实现：
  `path.length === 0` → 写 `node.ratio`（clamp 0.05–0.95），否则按 head 下钻。
- 测试：`tests/layout.test.ts` 三用例（根分割空路径、嵌套 [0]/[1] 写子节点、叶子/越界安全 + 收敛）。
  注意测试树 `b.a` 是叶子，`[1,0]` 应为无操作（b.ratio 不变）。

## B27 tab 区拖拽 = 排序（与分屏预览互斥）

用户要求：拖 tab 落在 tab 区 = 调整顺序；预览效果与分屏预览区分开。

- **语义**：tab 区（`.panel-tabstrip`，顶部窄条）落下 = 排序/移动，显示**插入指示线**
  `.tab-insert`（absolute 竖线，`.panel-tabstrip` 需 `position:relative`）；
  面板区落下 = 分屏预览（`.split-preview show zone-*`），两者互斥。
- splitview 新增 `stripOf/stripUnder/stripInsertInfo/showInsertIndicator/clearInsertIndicators`；
  `onTabDragMove` 先判 strip（显示插入线、清分屏预览）再 zoneOf；
  `onTabDragEnd` strip 分支 → `onMoveTabToStrip(panelId, tabId, beforeTabId)`（SplitviewCallbacks
  新必选回调）；面板区分支 `onDropTabToPanel` 可选属性调用需**双层可选链**
  `svCallbacks?.onDropTabToPanel?.(...)`（`svCallbacks?.` 只护第一层，TS2722）。
- main.ts `moveTabToStrip` 两个关键点：
  1. **同面板分支必须真的 splice 移动数组**（最初漏写只重绘 → 排序无效）；
  2. **同面板排序绝不能改 `dst.activeTabId`**——改了但不重挂视图会破坏 panel.viewTabId 不变量
     （状态显示新标签、编辑器仍是旧内容），后续激活因「已是活动」早退永远无法恢复。
     排序保持原激活不变（浏览器/记事本行为）。跨面板分支才置 `dst.activeTabId` 并 rebuildLayout。
- 测试：`tests/tabstrip-drag.test.ts`（stripInsertInfo 三用例 + 接线/互斥静态断言）；
  smoke 新用例「拖 tab 到 tab 区 = 排序」。**教训：smoke 用例不得假设面板布局**
  （`names.length === 2` 硬编码在多前置用例后必挂，改为捕获前置数量）。
- 全量 **128/128 通过（15 文件）**；cargo test 15/15；tsc/vite build 通过。

## B28 大纲分隔条样式统一（9609f18）

用户要求：大纲区分隔条样式与其他（分屏）分割条一致。

- 分屏分割条基准 = `.layout-sep`：5px、常显 `var(--border)`、hover 变 accent、z-index 5。
- `.toc-resizer` 旧为 4px 透明细条 → 改为与 `.layout-sep` 同款；`.toc-panel` 同时去掉自带的
  右侧边框线（与常显分隔条相邻会叠成双线）。
- 回归测试放 tests/regressions.test.ts（B28 静态断言）。**教训：断言 CSS 文本时注意两坑——
  ① `var(--accent, #06c)` 带回退值，`toContain("var(--accent)")` 匹配不上（逗号截断），用正则
  `/var\(--accent[,\)]/`；② 断言 `not.toContain` 时别把关键词写进被检 CSS 的注释里。**

## B29 源码模式大纲跳转贴底（ce0238c）

用户报告：源码模式点大纲跳转，标题行落在视口底部而不是顶部。

- **根因**：`dispatch({ selection, scrollIntoView: true })` 是 CM6 的**最小滚动**——目标行在
  视口下方时只滚到刚好可见（贴底）。
- **修复**：活动实例与其他可见实例的跳转改用
  `EditorView.scrollIntoView(pos, { y: "start", yMargin: 0 })`（滚动效果随 dispatch 的 effects
  一起传），标题行精确顶到视口顶部。预览态路径 `syncToLine` 本就顶部对齐，未动。
  转到行（goto-overlay）仍是 scrollIntoView:true，语义不同未改（如用户反馈贴底再改）。
- **测试**：smoke TOC 用例补运行时断言——拦截全部视图的 dispatch（实例属性遮蔽原型方法，
  用完 delete 恢复），点击大纲项后必须出现 effect value.y === "start"，且不得出现
  `scrollIntoView === true`。注意断言 spec 直接查对象属性，**不要 JSON.stringify**（可能循环引用）。

## B30 查找栏无法关闭 + 布局优化 + 预览态查找（ea06fa6）

用户报告：查找/替换悬浮条无法关闭；要求优化布局样式，且预览模式下有和源码一样的高亮与跳转。

- **无法关闭的根因（CSS）**：`.find-bar { display:flex }` 覆盖了 `hidden` 属性的 UA 样式
  （`display:none`）——`close()` 置 `dom.hidden=true` 后浮层依然可见。
  修复：`.find-bar[hidden] { display: none }`。**教训：author 样式设了 display 的元素
  不能靠 hidden 属性隐藏，必须补 [hidden] 规则。**
- **布局**：`.find-row` 之前根本没有 CSS 规则（行内元素裸排）→ 补 flex 行 + 窄窗换行；
  标题栏分隔线、按钮统一 padding/hover 描边、主操作 hover 反色、
  `.find-count` 补样式（DOM 类名与 CSS 的 .search-count 不一致，恒无样式）、
  命中列表 hover 左侧 accent 竖条。
- **预览态查找**：PreviewPane 新增 `applyFind(q|null)/stepFind(dir)/findState()`——
  对渲染后文本节点 TreeWalker 快照 → 按查询（大小写/全词/正则，语义与编辑器一致）分割
  包装 `<mark class="cm-find-match">`（复用编辑器高亮样式，视觉统一）；
  stepFind 环绕 + `scrollIntoView({block:"center"})`（jsdom 无此 API，用 `?.` 守卫）。
  main 层接线：applyFindQuery / stepFind（预览分支）/ refreshFindCount（预览计数）/
  clearFindHighlight / renderMarkdownFor 重放。范围非 doc 或文本为空 → spec=null 清除。
- **两个坑（测试抓出）**：
  1. 拆标记后必须 `parent.normalize()`——相邻文本节点不合并，下次匹配会在拆分边界上
     产生伪 `\b` 词边界（"foo"+"bar" 被当成两个词，全词匹配误命中 foobar 前缀）；
  2. setBlocks 复用 block 时旧 mark 留在节点里、被替换 block 带走标记，计数与 DOM 脱节
     → setBlocks 开头统一 clearFindMarks（重放由 main 层 patch 后做）。
- 测试：`tests/preview-find.test.ts`（命中数/大小写/全词/正则/清除还原/步进环绕/重渲染失效）
  + regressions B30 静态断言（[hidden] 规则、find-row flex、main 接线、复用 cm-find-match）。
- 全量 **134/134（16 文件）**；tsc/vite/tauri 通过。

## B31 转到行顶部对齐（717721b）

用户要求：「转到行」（Ctrl+G / F5）跳转也把目标行顶到视口顶部（与 B29 大纲跳转一致）。

- goto overlay 的 dispatch 改为 `EditorView.scrollIntoView(pos, { y: "start", yMargin: 0 })`；
  预览态路径 `syncToLine` 本就顶部对齐。
- 「插入文件路径」（insertDroppedPath）保留 `scrollIntoView: true` 最小滚动——光标处插入
  不应拽动视图，语义不同。
- regressions 补 B31 静态断言：以 `overlay.className = "goto-overlay"` 为锚截取 4000 字符区域，
  断言含 y:"start" 且区域不含 scrollIntoView:true（注意不要全局断言，插入路径处是合法保留）。
- 全量 **135/135（16 文件）**；tsc/vite/tauri 通过。

## B32 折叠按钮样式 + 折叠态开关文件窗口稳定（73ade89）

用户要求：优化 tab 区折叠按钮样式；折叠按钮显示时打开/关闭文件的效果有问题。

- **窗口漂移根因**：StripEntry.start 是**纯下标**，打开/关闭标签后数组位移，
  窗口看到的内容整体错一格（关掉左侧折叠标签最明显）。修复：`reanchorStart`
  按 tabId 锚定——锚点（原窗口第一标签）还在就停在当前位置；被关闭则退而求其次
  取原窗口内仍存在的最近标签。每次渲染都重对齐（滚轮改 start 后锚点不变，无副作用）。
- **激活跳变根因**：旧实现「活动标签下标变化 → start = activeIdx」——切换两个
  **可见**标签也会把窗口左对齐跳一下；新开文件（尾部追加+激活）只露出新标签自身。
  修复：可见性驱动最小移动——折叠在右侧尾部对齐（保留左侧上下文）；
  折叠在左侧**仅在激活事件时**（activeIdx !== lastActive）左移拉入，
  滚轮浏览不被拽回（applyOverflow 增加 activeChanged 参数区分）。
- **样式**：`.tab-more` 改为「» + 折叠数量徽标（.tab-more-count）」，
  hover 描边 + transition；活动标签被折叠时 `.tab-more-active` 高亮
  （描边 accent、徽标反色），tooltip 注明「含当前活动标签」。
- 测试：tabstrip-overflow 新增 4 用例（徽标与高亮 / 关闭左侧折叠标签不漂移 /
  可见标签间切换不跳 / 追加打开最小移动尾部对齐）；徽标结构断言更新。
- 全量 **139/139（16 文件）**；tsc/vite/tauri 通过。

## B33 多格式大纲 + 激活残留清空（a60d433）

用户要求：大纲支持更多格式（toml/json 等）；带大纲文件切到不带大纲文件时左侧大纲没清除
（跨面板、点击文档内容激活）。

- **多格式**：新模块 `src/markdown/outline.ts`，`extractOutline(name, text)` 按扩展名分派——
  md→extractToc；toml `[a.b]` 点分深度；ini/cfg/conf/inf/properties `[section]` 一级；
  yaml 缩进映射键（跳过 `-` 列表/注释/`---`）；json 逐行 `"key":` 扫描（level=花括号深度，
  容错 JSONC 注释、解析失败也能出；**level=depth 非 depth+1**；同行内联嵌套不提取）；
  py class/def（缩进/4）。返回 `null`=格式不支持（renderToc 空态文案区分
  「当前文档类型不支持大纲」/「当前文档没有标题」）。updateTocDrawer 改走 extractOutline。
- **激活残留**：jsdom 全路径实测清空正常（cm-content mousedown 也正常），无法复现桌面残留；
  工程兜底：onActivatePanel **即使面板未变化也幂等刷新** updateTocDrawer（原早退跳过）；
  closeTabById 激活新面板分支（原只 refreshTitle/Status/renderPanelTabs）补 updateTocDrawer。
  **教训：test 文件内多个用例共享模块状态（main 只 bootstrap 一次、btn-outline 是 toggle），
  转正 diag 用例时 outline.click() 会把上一用例打开的抽屉关掉 → updateTocDrawer
  因 hidden 早退 → 假阴性。多用例文件里先断言/确保 UI 状态再操作。**
- 测试：tests/outline.test.ts（6 用例）；toc-follows-panel 加「点击 cm-content 激活清空」回归。
- 全量 **146/146（17 文件）**；tsc/vite/tauri 通过。

## B34 应用改名 LitePad + 矢量重绘图标

用户要求：应用名改为 LitePad；重新生成替换图标，背景四角全圆角，样式简洁美观、贴合应用名与功能。

- **批量改名（sed 17 文件）**：`s/LitePad/LitePad/g; s/litepad/litepad/g`——src/main.ts
  （标题/关于/错误横幅/退出）、menubar/exporter、findbar（localStorage key
  litepad.findbar.pos）、theme（litepad.theme）、index.html、package.json（name=litepad）、
  tauri.conf.json（productName=LitePad、identifier=com.litepad.app、窗口 title）、
  Cargo.toml（name=litepad → exe 名 litepad.exe）、session/mod.rs（%APPDATA%\LitePad）、
  commands/mod.rs（litepad-smoke/app.log）、atomic_write.rs（临时目录前缀）。
  main.rs 模块内联无 lib crate 引用，Cargo 改名安全；gen/schemas、target 构建产物自动再生不手改。
- **图标重绘**：`scripts/gen_icons.py` 删除 from_source/icon_final.png（B25 AI 源图镜像方案
  废弃），新增 `draw_litepad()` 纯矢量自绘——圆角矩形 mask（radius=22.4%，
  rounded_rectangle 四角天然一致）+ #2563EB→#06B6D4 垂直渐变（逐行画线+mask composite）+
  白色便签纸 + 4 条淡靛文本线 + 斜放铅笔（独立画布正立绘制后 rotate(-32, expand)
  再 alpha_composite）。1024² 预览图 generated-images/litepad_icon_preview.png 视觉自查通过。
- **regressions 断言更新**：B25 用例改断言 rounded_rectangle/渐变色值/not icon_final.png；
  新增 B34 用例（11 个源码文件 not Match /[Ll]ite[Mm][Dd]/ + productName/identifier/package
  name 断言）。**教训：sed 改名后 docstring 里的旧方案说明文字也会被静态断言抓到
  （not.toContain("icon_final.png") 匹配注释），改方案要连注释一起清。**
- 全量 **147/147（17 文件）**；tsc/vite 通过；vite 偶发 SIGTERM 重试即过（老坑）。

## B35 git 发布流程 + 本地格式检查链（eee63f0 / 48f753d / cb4b610）

用户要求：完善 git 发布流程和本地格式检查流程，为推送 GitHub 做准备。
（AskUserQuestion 确认：存量代码一次性全量格式化 + 要 GitHub Actions release workflow。）

### 格式检查链
- **prettier 3.9.6**（printWidth=100，.prettierrc.json）+ .prettierignore
  （排除 dist/target/gen/generated-images/**test-data**/package-lock.json）；
  一次性 `--write` src/tests/scripts/index.html，独立 style(format) commit 与功能隔离。
- **eslint 10.10.0** flat config（eslint.config.js）：js recommended +
  typescript-eslint recommended；cjs 脚本豁免 no-require-imports/no-unused-vars；
  no-non-null-assertion off（CM DOM 集成大量使用）。存量 17 错误全修：
  main.ts `window as any`→结构化类型、restored 改 `.catch(() => false)`、
  sess 去掉无用初始化、panels→const；pipeline.ts any→**最小结构接口 InlineState
  （参数逆变天然兼容 StateInline）+ `ReturnType<MdInstance["parse"]>[number]` 取 Token**
  ——@types/markdown-it 是 export= 风格且包 exports 不含深路径，命名空间类型不可用。
- **rustfmt/clippy**（rustup component add 到 gnu 工具链）：cargo fmt 全量一次；
  clippy 2 警告修复（PanelSession derive(Default)、LineMatcher type alias）。
- **.gitattributes**：仓库统一 `eol=lf`；**test-data/** 必须 `-text`——
  CRLF/GBK/UTF-16 编码样例被 git 行尾转换会破坏测试语义（关键保护）。
- **hooks**：.githooks/pre-commit（prettier→eslint→tsc→cargo fmt --check，
  暂存文件过滤，目标 <30s）+ pre-push（vitest+cargo test 全量）；
  `git config core.hooksPath .githooks`。hook 内补 cargo 探测
  （$HOME/.cargo/bin、/c/msys64/mingw64/bin），找不到则警告跳过而非误报。
- npm scripts 增 lint/format/format:check/release。

### 发布流程
- **scripts/release.sh**：工作树校验 → 版本三处同步（package.json/
  tauri.conf.json/Cargo.toml + `cargo update -p litepad` 同步 lock）→
  `npm run build:all` → `chore(release): vX.Y.Z` commit → annotated tag →
  push 指引。支持 x.y.z / patch / minor / major，拒绝已存在 tag。
- **.github/workflows/release.yml**：push v* tag 触发；windows-latest +
  msys2/setup-msys2(MINGW64) + rustup 装 stable-x86_64-pc-windows-gnu
  （与本地一致，rust-toolchain.toml 锁定）→ npm ci → tsc/vite → tauri build
  → softprops/action-gh-release 附 NSIS + generate_release_notes。
  **首次实跑需调通（未验证）**。
- 基建脚本入库（此前一直 untracked）：build-all/build/dev.sh、run-vitest*.cjs、
  cdp_diag.mjs、screenshot.py、test-data/。

### 坑
- **npm install 被会话 SIGTERM 时会回滚 package.json+lock 并删除刚装包**
  （jsdom 被删导致 13 个 jsdom 测试文件被静默跳过——只收集 4 文件不报错！）。
  对策：PowerShell 通道跑 npm（bash shim 会触发 wsl.exe 拦截）；仍超时则手工写
  package.json devDeps + `npm install --package-lock-only --offline` 重建 lock。
- prettier 会重排 import → 「import { createFindBar」类静态断言失配 →
  断言只查标识符 `/\bcreateFindBar\b/`，不锁排版。
- tauri build（tauri.js 直调）与 vite build 各自偶发 SIGTERM，重试即过。

## B36 全仓库改名清理 + 发布 GitHub + LiteMD 空间归档

用户要求：①梳理项目中所有文件，把 LiteMD 改成 LitePad 并提交推送 GitHub；
②把 WorkBuddy 中所有 LiteMD 空间的会话与记忆总结并挪到 LitePad 空间。

### 改名（5d42a94）

B34 只改了 11 个源码文件，本次全量扫描发现**另 17 个文件仍有 41 处**
（`LiteMD` 30 / `litemd` 11）。用 python 按字节替换（保留原编码与行尾）：

- 文档：DESIGN / TASK / overview；《技术方案》一并 `git mv` 改名 LitePad-技术方案.md
- 脚本：build-all|build|dev 注释、vite.config.ts 插件名 `litemd-request-logger`、
  **screenshot.py 默认窗口关键字**（功能修复：窗口标题早已是 LitePad，旧默认值抓不到窗口）
- 样式/测试数据：global.css 注释、test-data/UTF8样例.md（该目录无测试引用，改名安全；
  已确认 6 个样例里只有它含 LiteMD，UTF-16/GBK 样例按各自编码逐字节扫过均无命中）
- **regressions.test.ts 的 B34 用例从「11 文件硬编码」升级为「全仓库文本文件扫描」**
  （SKIP_DIRS 跳过 dist/target/gen/node_modules；跳过二进制扩展名）。
  **关键取舍**：`.workbuddy/` 必须豁免——记忆与归档文件要能写下「原名是 LiteMD」这一史实，
  否则归档自相矛盾。
- 顺手修 eslint 配置缺口：`scripts/**/*.cjs` 未覆盖 `**/*.mjs`，导致 cdp_diag.mjs
  报 20 个 console/process no-undef（B35 遗留），补上后全仓库 eslint 0 错误。

### 发布（仓库 oh-myfun/LitePad，Public）

- `gh repo create oh-myfun/LitePad --public`（用户选定 Public）。
- **网络坑**：本机 **SSH 22 端口被拒**（`ssh: connect to host github.com port 22: Connection refused`），
  但 `ssh -p 443 git@ssh.github.com` 可认证成功。由于全局 git 已配
  `[http] sslVerify=false` + gh 凭证助手，**HTTPS 通道直接可用**（`git ls-remote` 实测通过），
  故 remote 用 `https://github.com/oh-myfun/LitePad.git`，无需改 `~/.ssh/config`
  （该文件含 DEVENV 保护区，不应手改）。
- 首次 push 被 pre-push 拦下 → 见下。

### pre-push 误拦的根因与修复（55b7039）

push 报「vitest 未通过」，但输出是 **147/147 全过**——真正的失败信号是 3 个
Uncaught Exception，让 vitest 以**非 0** 退出：

- `Range.getClientRects is not a function`：jsdom 24 未实现，CM6 在 mousedown 里会调。
- `ReferenceError: document is not defined`：menubar 的 MutationObserver 在 jsdom 拆除后仍回调。

根因是「**jsdom 桩靠每个测试文件各抄一份**」：smoke.bootstrap 与 view-switch-noedit
各自打了 Range 桩，**toc-follows-panel 漏打** → 它的 B33 mousedown 用例触发异常。
（09-12 曾判定这类错误「exit 0 无影响」，本次实测**会**导致非 0 退出、拦住所有推送。）

修复：新增 `tests/setup.ts` 集中 matchMedia / rAF / Range 三个桩，经
`vite.config.ts` 的 `test.setupFiles` 全局生效（defineConfig 改从 `vitest/config` 导入
以获得 `test` 字段类型）；删掉两处重复贴片；menubar 观察者回调补
`typeof document === "undefined"` 兜底并 disconnect。
**教训：环境桩必须单点收口，靠复制粘贴迟早漏一处，而漏的后果是整条推送链断掉。**

### 记忆体系重建 + 空间归档（2d9f9a7）

- 旧工作区 `E:\Project\LiteMD` 在 WorkBuddy 留下孤立空间
  `~/.workbuddy/projects/e-Project-LiteMD/`（1 个会话 9439 行 / 约 36MB，
  2026-09-10 23:25 → 09-13 12:06，**178 轮用户输入**，就是本项目全部开发史）。
  用 python 解析 jsonl 提取全部用户需求（`<user_query>` 提取 + 过滤 system-reminder 注入）。
- **MEMORY.md 由 16KB/179 行重写为索引式摘要**（原文件含重复条目——「状态归 Rust」重复两次、
  待办清单把 B14–B27 重复罗列两遍——且超长被注入时截断）。新增三个专题文档：
  `ARCHITECTURE.md`（架构不变量/踩坑根因）、`BUILD-ENV.md`（构建与沙箱精确命令）、
  `LiteMD-Space-Archive.md`（旧空间会话与记忆归档，含 178 轮需求分五阶段时间线）。
- 空间实体认知（有价值）：WorkBuddy 的「空间」= `~/.workbuddy/projects/<工作区路径 slug>`，
  按目录名映射工作区；`~/.workbuddy/sessions/*.json` 只是在跑进程的心跳、不是会话索引；
  空间级记忆 profile 在 `~/.workbuddy/memory/<uid>_memory.md`（服务端托管，本地写入会被覆盖，
  故只能登记其「仍停留在 M3 前且未更名」的事实，不能本地改写）。
- 用户选择：仓库 **Public**；旧空间数据**保留不动**（只做总结与归档，不触碰原始数据）。



### B36 收尾：release 产物重建

`cargo clean` 连带清掉了旧的 release 产物（exe + NSIS），按约定重建：

- tsc 0 错误；**vite build 首次挂死**（后台跑 + `| tail` 的管道收尾问题，
  子进程被 SIGTERM 后管道仍被持有 → 任务假 running、`dist/` 只剩 index.html）。
  判据：`target/`、`dist/` 近 2 分钟无文件更新。**改为落文件 `> /tmp/x.log` 重试即过**。
- tauri build --release 成功（38m28s，冷启动）：
  `litepad.exe` 9.17 MB + **`LitePad_0.1.0_x64-setup.exe` 5.99 MB**
  —— 安装包文件名已随更名正确变化（原为 `LiteMD_0.1.0_x64-setup.exe`），
  等于端到端验证了改名对打包链路的影响。
- BUILD-ENV.md 增「长构建不要配管道 + 落文件判活法 + 实测耗时参考」。

### 本轮最终结果

- GitHub：https://github.com/oh-myfun/LitePad（Public，main 已同步，5 个提交）
- 记忆：LitePad 空间现有 MEMORY.md（索引）+ ARCHITECTURE.md + BUILD-ENV.md
  + LiteMD-Space-Archive.md（旧空间归档）+ 逐日日志
- 旧空间 `~/.workbuddy/projects/e-Project-LiteMD/` 按用户选择**保留不动**

## B37 版本号随开发更新 + 打通 GitHub 发布流水线

用户反馈：随开发要更新版本号；GitHub 上没触发编译和发布流程。

### 根因

`release.yml` **只由 `v*` tag 触发**，而仓库**从未打过任何 tag** —— `gh run list` 为空、
`gh workflow list` 显示 Release 为 active 却零运行。只推 main 永远不会发布。
（v0.1.0 从 M0 一直挂到 B36，也说明「版本号随开发走」缺一个可执行的约定。）

### 决策（AskUserQuestion）

版本 0.1.0 → **0.2.0**（M0–M3 + B4–B36 算一个完整阶段，不抬 1.0 以保留正式发布空间）；
触发策略**三种全加**（tag 发布 + main CI + 手动 dispatch）。

### 改动（4ab7ecf / 4e6015d）

- 新增 `.github/workflows/ci.yml`：push main / PR / 手动 → format:check → lint → tsc →
  vite build → vitest → cargo test，与本地 .githooks 对齐；concurrency 取消同分支旧运行。
- `release.yml` 加 `workflow_dispatch`（可填 tag 补发）；checkout 与 `tag_name` 跟随该输入，
  避免重跑时构建错 ref（否则会把 main 分支当成发布对象）。
- `scripts/release.sh` 加 `--ci`（跳过本地全量构建，交给 Actions）；文档里写明「必须推 tag」。
- `package.json` lint 脚本补 `scripts/*.mjs`（此前漏扫 cdp_diag.mjs）。
- **新增「版本号四处同步」回归断言**：package.json / tauri.conf.json / Cargo.toml / Cargo.lock。
  版本散落四处，漏改会让 tag、安装包名、关于对话框三者对不上，而 Actions 只认 v* tag。

### 结果

- CI：**6m41s success**（所有步骤全绿）；Release：**8m27s success**。
- Release 已上线：https://github.com/oh-myfun/LitePad/releases/tag/v0.2.0
  附件 `LitePad_0.2.0_x64-setup.exe` 5,983,797 B（本地构建 5,985,773 B，差异极小即环境差异）。
- 两条流水线所有步骤 conclusion 均为 success，**B35 遗留的「workflow 首次实跑未验证」已消除**。
- 提交拆分：`ci(release): ...` 与 `chore(release): v0.2.0` 各一个 commit（版本回退到 0.1.0
  再重 bump 以保持每个提交自洽）。本地质量门：prettier/eslint/tsc/lint 全过，vitest 148/148。

### 坑与约定

- Release 的两条流水线都必须装 MSYS2 MinGW64 + `rustup default stable-x86_64-pc-windows-gnu`，
  否则 GNU 宿主链接不了（本项目无 MSVC）。
- Actions 侧比本机快得多（6.7 / 8.5 分钟 vs 21 / 38 分钟），**发布优先走 Actions**。
- BUILD-ENV.md 增「GitHub 流水线」章节（触发矩阵、重跑方式、耗时、查询命令）。

## B38 图标重制（脚本重写为多变体生成器）

用户原话：「重新生成一个漂亮点的图标」。「漂亮」属主观判断，故**先出候选再定**，不一次押注。

### `scripts/gen_icons.py` 重写要点

- 弃用 B34 的单函数渲染，改为**多变体 + 对比图**结构：
  `VARIANTS = {A,B,C}` → `render(letter)`；`python scripts/gen_icons.py` 出
  `generated-images/icon_variants.png`（三列：大图 / 128·64·48·32·16 一排 / 浅色底·深色底对照），
  `--install <A|B|C>` 才写入 `src-tauri/icons/{32x32,128x128,128x128@2x,icon.ico}` + 单图预览。
- **质感升级**（相比 B34 的纯平色块）：
  - 45° 对角渐变蒙版用「竖直 ramp ⊕ 水平 ramp 求平均」（`ImageChops.add(scale=2.0)`）合成，
    **不用 rotate**——旋转 expand 会在角上留黑边，此法无伪影且快。
  - 左上径向柔光 + 右下暗角（`Image.radial_gradient` 反转后 paste 偏移）+ 白色内描边 → 玻璃感。
  - 卡片与工具都加高斯模糊投影（`_shadow_from_alpha`），出层次。
  - **圆柱高光 `_cyl(edge, mid)`**：`abs(2v-255)` 做蒙版，上下边缘偏暗、正中偏亮，
    铅笔杆/橡皮/金属箍/钢笔杆都有体积感。
  - 所有层最后用 tile mask 乘一遍 alpha（`ImageChops.multiply`），保证圆角干净、柔光不溢出。
- 三个变体：**A** 便签纸+铅笔（B34 精修，首行改品牌渐变标题条）、
  **B** 折角文档+钢笔（`_card(cut=)` 削右上角 + 背面折角三角）、**C** 字母 L+琥珀光标。
- 迭代中修掉两处观感问题：B 的钢笔太粗、笔尖像毛笔 → 直径 0.086s→0.074s 并加握位/色环；
  C 的光标太短粗像电池 → 收成 0.040s×0.328s 的高瘦插入符。
- 中文字体走 `_font()`：msyh.ttc → segoeui → arial → `load_default(size=)`，避免对比图文字缺失。

### 运行方式（不变）

`C:/Users/maoyu/.workbuddy/binaries/python/envs/default/Scripts/python.exe scripts/gen_icons.py`
（Pillow 在该 venv 内）。

### 待办

- 用户选定 A/B/C 后执行 `--install`，再 **重跑 `tauri build` 才进 exe**（B35 约定）；
  已发布的 v0.2.0 安装包内仍是旧图标。

## B39 查找栏收敛为「当前文档」

用户原话：「查找替换悬浮栏无需查找文件夹功能，也不用下拉菜单来选择查找范围」。

### 范围判定（关键）

「范围」下拉是切换查找范围的**唯一**入口，去掉它 = 查找能力收敛到**当前活动文档**；
因此「所有打开的文档」与「文件夹…」两项一并下线，不是只删文件夹那一项。
这是 M2「跨文件搜索」交付物的下线：UI 撤掉后后端就成了不可达残骸，故**全链路删除**。

### 改动

- `src/shell/findbar.ts`：`FindBarQuery` 去 `scope`/`folder`；回调去
  `onSearchAll`/`onOpenHit`/`onPickFolder`；句柄去 `setHits`/`setFolder`，`open(seed?)` 单参；
  DOM 去范围 `<select>`、搜索目录输入、浏览…、「查找全部」、结果列表；三个选项复选框移到新的
  `.find-row-opts` 行。`open()` 现在**总是**同步一次查询（原逻辑只在 doc 范围同步）。
- `src/main.ts`：删 `defaultSearchDir`/`jumpToHit`/`searchAllInScope`/`openFindHit`；
  `replaceAllInScope`（可跨文档写回）→ `replaceAllInDocument`（只改活动视图 + 快照）；
  `openFindBar(scope?, focus?)` → `openFindBar(focus?)`；删 Ctrl+Shift+F 分支；状态栏提示改 Ctrl+F。
- `menubar.ts` 删「在文件中查找」项与 `onFindInFiles`；`keymapdialog.ts` 删 Ctrl+Shift+F 行。
- `global.css` 删 `.find-scope`/`.find-folder`/`.find-results`/`.find-hit*`/`.find-primary`，
  新增 `.find-row-opts`。
- 后端全链路删除：`commands/mod.rs` 的 `search_files`+`SearchHit`+`collect_files`+
  `search_one_file`+`LineMatcher`+`SEARCH_MAX_*`（整块 5445 B）、`main.rs` 注册、
  `api.ts` 的 `searchFiles`/`SearchHit`；**`Cargo.toml` 的 `regex = "1"` 依赖一并移除**
  （它只服务于跨文件搜索）→ Cargo.lock 只少了 litepad 的 regex 依赖行；
  release exe 5.98 MB → **5.5 MB**。
- 三处 jsdom 集成测试（smoke.bootstrap / toc-follows-panel / view-switch-noedit）的 api mock
  去掉 `searchFiles` 键。

### 测试

- `tests/findbar.test.ts` 重写为单文档契约：断言无 `.find-scope`/`.find-folder`/`.find-results`/
  `.find-hit`，按钮恰为 `×/↑/↓/替换/全部替换`，查询对象键恰为
  `caseSensitive/regexp/replace/text/wholeWord`，替换控件不再有禁用态，种子文本只同步一次查询。
- `menubar.test.ts`：编辑菜单去掉「在文件中查找」并**反断言**其不存在；新增 B39 块静态断言。
- `regressions.test.ts` 新增 B39 全链路移除断言（含 Rust 与 Cargo.toml，防残骸回流）。
- 结果：vitest **151/151**（原 148 + 新增 3）、cargo test 15/15、prettier/eslint/tsc/cargo fmt 全过；
  `npm run build:all` 成功（release exe 7.8 MB + NSIS 5.5 MB）。

## B40 修正：跨文档查找保留，改为勾选框

用户原话：「查找所有打开的文档的功能还是要有的，只不过不是用下拉菜单选择，直接勾选就行」。

即 B39 砍过头了：**文件夹搜索要删，跨文档查找要留**，只是入口从下拉菜单换成勾选框。

### 改动

- `findbar.ts`：新增勾选框「所有打开的文档」（`.find-opt-docs`，在 `.find-row-opts` 里靠右，
  前面加 `.find-spacer` 顶开），查询对象加 `allDocs: boolean`；恢复 `FindHit`、`onSearchAll`、
  `onOpenHit`、`setHits` 与结果列表；勾选时才显示「查找全部」并改 placeholder，
  回车改为执行跨文档搜索；查询文本一变就作废上一次结果（避免展示过期命中）；
  取消勾选即 `setHits([])` 清空。
- **`onSearchAll` 由 async 收为同步**——现在只扫已打开文档的内存快照，不再有 IPC，无需 Promise。
- `FindHit` 去掉 `kind`（文件夹搜索已删除，命中必然来自已打开文档），`docId` 变必填。
- `main.ts`：新增 `searchOpenDocs`（扫标签快照）与 `openFindHit`（切到该文档、选中命中、滚到中间）；
  `replaceAllInDocument` → `replaceAllInScope`，按 `q.allDocs` 走「只改活动视图」或
  「遍历全部已打开文档（优先派发给挂载中的实例，离屏则直接改快照并置脏）」两条分支；
  勾选态下 `refreshFindCount` 清空单文档「第 n/m 处」（改由结果列表给「N 条结果（M 个文档）」）。
- `global.css`：恢复 `.find-results`/`.find-hit*`，新增 `.find-spacer`、`.find-opt-docs`。
- **后端依旧保持 B39 的删除状态**：`search_files`（读盘递归搜索）、`searchFiles` API、
  `regex` 依赖都不回来——跨文档查找走内存，不需要它们。

### 测试

- `findbar.test.ts` 扩到 16 项：断言查找栏内**不存在任何 `<select>`**、没有 `.find-scope`/
  `.find-folder`，但必须有 `.find-opt-docs`；覆盖「未勾选不显示查找全部 + 回车为步进」、
  「勾选后回车/点按钮执行跨文档搜索并渲染结果、点结果回调 onOpenHit」、「空查询给提示不搜索」、
  「取消勾选清空结果并收起按钮」、「查询变更作废过期结果」。
- `regressions.test.ts` / `menubar.test.ts` 的 B39 块改名 B39/B40：保留「无下拉、无文件夹搜索」，
  改为**反断言 + 正断言并存**（必须有 `find-opt-docs` 与 `find-results`）。
- 结果：vitest **156/156**、cargo test 15/15、prettier/eslint/tsc/cargo fmt 全过；
  `npm run build:all` 成功（release exe 7.8 MB + NSIS 5.5 MB）。

## B41 图标定稿：方案 B（无背景）+ 钢笔调头 + 修复图标不入 exe 的坑

用户原话：「图标选方案B，但是不要加背景，直接显示折角文档+钢笔进图案，
铅笔方向调整下，笔头朝左下角，笔尾在右上角」。

### 设计改动

- **去掉圆角渐变底**：`variant_b` 不再调 `_tile()`，直接在透明画布上画文档 + 钢笔，
  也返回时不再走 `_finish()`（没有 tile mask 可乘）。
- **钢笔调头**：`_tool` 正立绘制时局部 +x 就是笔尖方向，实测 `rotate(-θ)` 会让 +x 指向
  「屏幕坐标下顺时针 θ 度」。故笔头朝左下角 = +x 指向 135° 方位 → **`rotate(-135)`**
  （B38 的 `-36` 是笔头朝右下）。
- 无背景后白纸会与浅色底糊在一起，故 `_card` 支持 `outline` / `fold_edge`：
  纸面加极淡 #CBD5E1 描边（会被 mask 裁掉折角处），折痕用 #A5B4FC 勾边，再配柔投影。
- 文本行加深：首行 #6366F1（标题兼对比锚点），其余 #A5B4FC（原 #C7D2FE 在纯白上太淡）。
- 钢笔收细：直径 0.074s → 0.064s，长度 0.78s，中心 (0.524, 0.524)（原 0.500 会压住折角）。
- `write_single_preview` 改成**预览板**：透明底棋盘格 + 浅色底 / 深色底对照 + 尺寸梯队
  ——无背景方案必须同时看两种底色才能判断，单张透明 PNG 在图片查看器里看不准。

### 踩坑（重要，已记入 BUILD-ENV.md）

- 换完图标跑 `npm run build:all`，exe 里**仍是旧图标**。用「从 exe 里按 PNG 签名抠出
  内嵌图标、和 icon.ico 逐条 sha256 比对」的办法定位到：`out/resource.rc` 与
  `out/libresource.a` 还停在上一版（26174 B），即 **build script 没重跑**。
- 根因：`tauri-build` 只为 tauri.conf.json / capabilities 声明 rerun-if-changed，**不盯 icons 目录**；
  增量构建下改图标不触发资源重编译，且**不报错**。
- 修复：`src-tauri/build.rs` 显式 `println!("cargo:rerun-if-changed=icons")`。
  重建后 libresource.a → 30558 B，exe 内 7 个尺寸全部与 icon.ico 字节级一致。
- 另记：PowerShell 的 `Add-Type` 在本会话被安全策略拦截，校验二进制改用 Python 更省事。

### 验证

- exe 内嵌图标 7 个尺寸（16/24/32/48/64/128/256）与 `src-tauri/icons/icon.ico` **逐条 sha256 相同**。
- `npm run build:all` 成功：release exe 7.8 MB + `LitePad_0.2.0_x64-setup.exe` 5.5 MB（21:46）。
- 已知取舍：无背景方案在 16px 及以下辨识度弱（无底板撑住轮廓），这是去背景的固有代价。

### 收尾

- 提交 `d112e46`（feat(icon)，B41），pre-commit 通过；`git push origin main` → pre-push 全绿
  （vitest 156/156、cargo test 15/15）；CI run 34760928060 **success**（3m45s）。
- CI 遗留告警：`actions/checkout@v4`、`actions/setup-node@v4` 仍跑 Node 20，GitHub 已标弃用
  （未来会强升 Node 24）。非阻塞，可择机升 v5。
- 备注：`generated-images/` 在 `.gitignore` 里，预览板 `litepad_icon_preview.png` 是副产物不入库。

---

## B42 菜单重组：新增「设置」菜单 + 可编辑快捷键（提交 e24cd37）

### 用户需求

查看菜单去掉 主题/预览行距/大纲宽度/分屏；文件菜单去掉 新建文件默认行尾/默认编码；
帮助菜单去掉 快捷键。这些被移除的项统一收进**新的「设置」菜单**，设置含两项：
**首选项**（承载上述预设值）与**快捷键**（支持浏览 + 编辑软件快捷键）。
另要求给 **大纲、折叠展开** 加默认快捷键。

澄清后确定的取舍：**分屏只保留快捷键**（不再出现在菜单）；**首选项用二级子菜单**（非弹窗）。

### 实现要点

- 菜单变为 **文件 / 编辑 / 查看 / 设置 / 帮助**（助记键 F E V S H，`SETTINGS_MENU_INDEX=3`）。
- `src/shell/menu.ts` 新增 `submenu` 能力：`MenuItem.submenu?: MenuItem[] | (() => MenuItem[])`。
  与既有「二级弹出」的关键区别是**扁平挂到 `document.body`**（用父按钮 rect 定位、右侧越界则左翻），
  这样不会被上层菜单的 `overflow` 裁掉；`chain`/`created` 数组维护展开层级，
  `closeDeeperThan(level)` 收深层，同层兄弟 `mouseenter` 换支，菜单滚动也收深层。
- 新增 `src/shell/keymap.ts`：统一命令注册表 `CommandDef{id,label,group,keys[],editable,force}`。
  解析**按 `KeyboardEvent.code` 而非 `e.key`**——`Ctrl+Shift+[` 的 `e.key` 是 `{`，
  按 key 会解析错。`KeymapOverrides`（命令 id → 键位串，`""` = 显式解绑），
  `Settings.keymap` 前后端双向持久化（Rust 侧 `HashMap<String,String>`，带 `#[serde(default)]`）。
- 新增大纲/折叠默认键位：切换大纲 `Ctrl+Shift+O`、折叠 `Ctrl+Shift+[`、展开 `Ctrl+Shift+]`、
  折叠全部 `Ctrl+Alt+[`、展开全部 `Ctrl+Alt+]`。分屏 `Alt+Shift+→ / ↓ / ←`。
- `keymapdialog.ts` 从只读列表重写为**浏览 + 编辑**面板：搜索、分组、改键（录键态「按下新键…」）、
  冲突拦截、Delete/Backspace 解绑、`恢复全部默认`。**录键监听挂在 `document` 捕获阶段**
  （不能挂按钮——按钮未必有焦点）；Esc 仅在非录键态关闭面板。

### ⚠️ 重大发现：默认键位与 CodeMirror 内置键位冲突（已写入回归测试）

用真实 `defaultKeymap` 探针实测确认（不是推测）：

- Markdown 下 CM 的 `Mod-/`（切换注释）会吞掉应用的 `Ctrl+/`，且**会往文档里插 `<!-- -->` 改写内容**；
- `Shift-Alt-ArrowDown`（整行复制）会吞掉应用的 `Alt+Shift+↓`（垂直分屏）。

对策（两者都必需）：

1. 全局 `keydown` 从冒泡改为**捕获阶段 + `stopPropagation`**，先于 CM 拿到事件；
2. `shortcutApplies(id)` 对**非 Markdown 文件放行 `view.toggle`**，保留 CM 原生注释切换。

并把 CM 这两条行为**锁进** `tests/editor-keymap-conflicts.test.ts`（3 例），
同时把 CM 的 `Ctrl+/`、`Alt+Shift+↓` 注册为**只读**键位项参与冲突检测
（`duplicateKeys` 跳过 `editable===false`）。

### 测试与门禁

- 新增 `tests/keymap.test.ts`(28) / `tests/keymapdialog.test.ts`(11) / `tests/editor-keymap-conflicts.test.ts`(3)；
  重写 `tests/menubar.test.ts`(18) 适配五菜单与子菜单；`regressions`/`smoke.bootstrap` 各增补 B42 用例。
- 全量 **216 用例 / 20 文件通过**；tsc / eslint / prettier / rustfmt 全部 0 报错；
  cargo test 15/15。pre-commit 与 pre-push 钩子均通过。
- 提交 `7cb3fbe`（原 `e24cd37`，amend 过一次），推送 `94be9b7..7cb3fbe`。
- **amend 原因**：首版提交信息里写了「README 快捷键说明已更正」，但**本仓库根本没有 README**，
  属不实描述。因为只是 tip 提交的信息问题、无内容改动，改用 `--amend` + `--force-with-lease` 修正，
  而不是留一句假话在历史里。教训：**写提交信息时不要写未经核实的文件/行为**。

---

## B43 图标微调：钢笔缩短 / 笔尖收进文档中间靠下 / 去掉文档投影（提交 92d6acc）

### 用户反馈（三条）

① 钢笔太长；② 笔尖不用顶到文档角落，落在「文档中间靠下点」就行；
③ 去掉折角文档的投影（点名：右上角折角处 + 下面两个圆角处）。

### 做法

- 先把方案 B 散落的魔法数字抽成常量：`B_CARD / B_PEN_LENGTH / B_PEN_CENTER /
  B_PEN_ANGLE / B_CARD_SHADOW`，`variant_b()` 读常量渲染。
- **关键几何**：钢笔是固定 135° 对角线，`笔尖 = 中心 + (长度/2)·(-0.707,+0.707)`，
  所以**改长度会同时移动笔尖与笔尾**。想只收笔尖必须同时反算中心。
  - 长度 0.78 → **0.62**；中心 (0.524, 0.524) → **(0.650, 0.400)**
  - 得到笔尖 (0.431, 0.619)：文档中心 (0.458, 0.490) → 恰好「中间靠下」，
    距左边缘 0.199、距下边缘 0.211，不再压角。笔尾 (0.869, 0.181)。
- **投影**：新增 `B_CARD_SHADOW` 开关并置 `False`（原来那段
  `_shadow_from_alpha(cm, ..., (15,23,42), 118)` 保留在开关后面，便于回退）。
- **验证是否为同一处阴影**：做了「折角处放大 + 前后对比」的探针图（棋盘格垫底看透明区），
  确认用户看到的就是**文档投影**（折角右下方与纸下缘最明显）；折角那块浅蓝三角是
  纸的**折背面造型**（`PAPER_FOLD` + 折痕），与投影无关，**予以保留**。
  对比图留在 `generated-images/litepad_icon_fold_before_after.png`（该目录 gitignore）。

### 踩坑与结论

- 探针脚本（渲染长度/落点候选 + 叠加笔尖笔尾标记 + 局部放大）是**必须的**：
  光看代码里的归一化数字判断不出「笔尖到底落在纸的哪里」，必须先渲出来看。
  用完即删，不入库。
- **`windres: preprocessing failed` 是瞬时故障**：push 时 pre-push 的 cargo test 在
  build script 处挂（而同一图标刚用 `tauri build` 成功打包过）；直接重跑 `cargo test`
  即 15/15 通过，再 push 也过。已记入 `BUILD-ENV.md`——**遇到先重试，别去改 .rc 或图标**。
- 新增回归用例（`tests/regressions.test.ts`）：断言 `B_CARD_SHADOW = False`、
  钢笔画长落在 [0.5, 0.66]、并按公式复算笔尖落点必须「在纸内 + 中线以下 + 不偏离中线过远」。

### 验证

- exe 内嵌 7 个尺寸（16/24/32/48/64/128/256）与 `icon.ico` **逐条 sha256 一致**。
- 全量 **217 用例 / 20 文件通过**；tsc / eslint / prettier 均 0 报错。
- release 产物已重出：`litepad.exe` 7.8 MB、`LitePad_0.2.0_x64-setup.exe`。
- 提交 `92d6acc`，推送 `1c7d4e5..92d6acc`。

### 遗留（已向用户说明）

- **钢笔自身的投影仍在**（笔身周围那圈柔影，会略微溢出纸外到透明区）。
  用户这次只点名了「折角文档下的阴影」，故未动。若用户要彻底扁平化，
  两个选项：整体去掉，或把笔影**裁剪到纸内**（笔仍贴纸、但透明区无 smudge）。

---

## B44 标签栏折叠态不重算（提交 d6f92c0）

### 用户报告

标签处于**折叠状态**时，缩放窗口 / 滚动鼠标 / 关闭 / 新建·打开文件，显示的标签项都不自动调整。

### 四条根因（全部先用失败用例钉住，再改代码——实测证据如下）

| 现象 | 实测 | 根因 |
|---|---|---|
| 缩小窗口 | 仍显示 6 个 | **完全没有尺寸监听**：`applyOverflow` 只在渲染时算一次，无任何 resize/RO 挂钩 |
| 放大窗口 | 仍折叠，不放出 | 同上 |
| 向左滚轮 | 窗口停在 tab5/tab6 | 活动标签「尾部对齐」规则**无条件**生效，滚完立刻被拽回 |
| 关闭标签 | 只剩 `tab3`（预算够 2 个） | `start` 被夹到末尾 → `fitCount` 只返回 1 |

「新建/打开」经实测**本来就是对的**（新标签是活动标签，尾部对齐会拉进来），
但仍补了用例加锁防回归。

### 修复（src/shell/tabstrip.ts）

1. `watchStripSize()`：优先 `ResizeObserver` 观察标签栏**自身宽度**
   （关键：拖分屏分隔条使面板变窄**不触发** `window.resize`，只用 window 监听会漏），
   无 RO 时退化 `window.resize`；多次触发用 rAF 合并。
   `WeakMap` 不可遍历 → 另存 `liveStrips: Set`，注册与重排时剔除 `!isConnected`
   （`rebuildLayout` 整棵重建面板 DOM，不剔除会累积已卸载节点）。
   附带修好「strip 在插入 DOM 前渲染 → `clientWidth=0` → 全部展开不折叠」。
2. 活动标签**左右两侧**的拉回一律门控在 `activeChanged`（左侧本来就有，右侧漏了）。
3. 新增 `fitCountFromEnd()`：窗口没铺满预算时整体左移补满；**已铺满则不动**，
   避免把用户滚出来的位置拽走。

### 方法上的收获

- 这次**先写失败用例**是关键：读代码时对「关闭」到底坏在哪有三种猜测，
  用例一跑直接给出了 `expected ["tab3"] to deeply equal ["tab2","tab3"]`，
  一眼锁定 `fitCount` 只返回 1。比继续推演快得多。
- 测试桩的 `STRIP_W` 由 `const` 改 `let`，再加 `afterEach` 复位，
  就能用 `dispatchEvent(new Event("resize"))` 模拟窗口缩放；
  因 `setup.ts` 把 rAF 换成了 `setTimeout(0)`，等一个宏任务即可同步断言。

### 验证

- `tests/tabstrip-overflow.test.ts` 新增 5 例（缩窗 / 放窗 / 向左滚轮 / 关闭补满 / 打开新标签）。
- 全量 **222 用例通过**（原 217 + 5）；tsc / eslint / prettier 均 0 报错。
- 提交 `d6f92c0`。

---

## B45 关闭非活动标签会闪一下（提交 a5e8c42）

### 用户报告

激活的文件和要关闭的文件不是同一个时，点关闭会闪一下——
先切到要关闭的文件，关掉，再切回原来激活的文件。

### 两处根因（都在 closeTabById）

1. **为了复用 `doSave` 而先切标签**：`doSave` 只保存**当前活动标签**，
   于是关闭前先 `switchTab(panel.panelId, tabId)` 把待关标签激活。
   `switchTab` 是**同步**的，而保存确认的 `await ask(...)` 在其后——
   浏览器在 await 处就会重绘，中间态真的被画出来了，这就是「闪一下」。
   → 改用 `saveDocCore(doc, tab, false)`：它本就按**指定实例**保存
     （实例正显示在面板上才从视图写回，否则用标签快照），不需要先激活。
     同时去掉 `activePanelId` 的切换（关别的面板的标签不该跳面板）。
2. **关完又无条件切到邻居**：`panel.activeTabId = nextId` + `view.setState(...)`
   对后台标签也一样执行 → 「平白跳到另一个文件」（闪一下的后半段）。
   → 新增 `wasShown`（`panel.viewTabId === tabId || panel.activeTabId === tabId`）：
     只有关掉的是正显示的标签才换显示内容，关后台标签只 `renderPanelTabs`。
   附带收益：「关闭其他 / 关闭右侧」走 `closeTabsSequentially` 逐个关闭，
   旧实现会逐个切换，**闪得更明显**；现在全程不动显示内容。

### 关键做法：回归用例要能证明它抓得住 bug

用例（smoke.bootstrap）在**点下 × 之后同步采样**视图内容——旧实现此刻已经切过去了。
写完先跑一次（通过），然后**临时把 `switchTab` 那行加回去**再跑，
断言以 `expected '' to be '# 第一题\n\n正文\n\n## 第二题\n'` 失败，
确认锁的是根因①而非别的，再把临时改动撤掉。
（**注意：不要用 git stash 做这种临时回退**——09-13 有过 .git 丢失事故；
直接编辑文件加回去、验证、再编辑撤掉最安全。）

### 验证

- 全量 **223 用例通过**（原 222 + 1）；tsc / eslint / prettier 均 0 报错。
- 提交 `a5e8c42`。

## B46 首选项弹窗化（设置窗口 + 精细选项）

用户原话：所有快捷键都要支持修改。首选项改成弹出设置窗口，包含更加精细的设置选项（比如行距，字体）。

- 「所有快捷键都要支持修改」B42 已达成（keymap 注册表 + 快捷键对话框），本次只需验证无遗漏。
- **新文件 `src/shell/preferencesdialog.ts`**：镜像 keymapdialog 模态骨架（settings-overlay/settings-dialog），
  分五组：外观(主题) / 字体与行距(编辑器字体+字号+编辑器行距) / 编辑器(自动换行+自动保存) /
  Markdown 预览(预览行距+大纲宽度) / 新建文件(默认行尾+默认编码)；每个控件 change 即回调 setter
  （即时生效+即时持久化，无保存按钮）；「快捷键…」按钮先关本窗再开 keymap 对话框（避免双层蒙层）。
- **menubar.ts**：删除 preferenceItems 子菜单与 12 个偏好回调，设置菜单 = 首选项… + 快捷键… 两个叶子；
  MenuBarCallbacks 新增 onPreferences。
- **main.ts**：新增 setFontFamily/applyFontFamily（写 --font-editor，空串回落 --font-mono；带空格族名加引号）、
  setEditorLineHeight/applyEditorLineHeight（--editor-line-height）、setFontSizeValue（绝对值字号）、
  setWordWrap/setAutosave（绝对值开关，toggleWrapSetting/toggleAutosave 改为其包装）；
  bootstrap 时应用 font_family/editor_line_height。
- **Settings 新字段**：`font_family: String`（空=默认栈）、`editor_line_height: f64`（1.0–2.5 默认 1.5），
  TS 接口与 Rust struct + Default 同步；serde(default) 前向兼容。
- **CSS**：`.editor-host .cm-editor` font-family 改 var(--font-editor, var(--font-mono))；
  新增 `.editor-host .cm-content { line-height: var(--editor-line-height, 1.5) }`；
  `.preferences-dialog`(460px) + `.preferences-group` 吸顶分组标题。
- **测试**：menubar.test.ts 重写设置菜单断言（两个叶子、无 has-submenu）+ 新增弹窗接线静态断言；
  regressions.test.ts B42 describe 更新（预设值改查 preferencesdialog.ts；新增 font_family/editor_line_height
  前后端成对断言）；新建 tests/preferencesdialog.test.ts（3 用例：渲染+初值/十项 change 回调/快捷键按钮先关窗+三种关窗方式）。
  vitest 224 全绿；cargo test 15/15；tsc/eslint/fmt 过。
- **坑（本会话第 2 次）**：同一消息里**并行批量 Edit 同一文件时部分编辑假成功不落盘**——
  main.ts 的 import/setAutosave/setupMenuBar 三处、menubar.ts 三处、mod.rs Default 均首次未写入，
  tsc/cargo 编译错误暴露后才补。**教训：改同一文件的多处内容要么逐条串行 Edit，要么 Edit 后立即 grep 验证落盘。**
- UI 效果（弹窗样式、字体切换）沙箱内无法冒烟，待用户桌面验证。
