# 2026-09-18

## B69：热退出开着时，空的未命名文档也要能跨重启回来

### 用户反馈

> 开启热退出时，如果又空的新建文档，下次打开也要能恢复

即：Ctrl+N 新建、还没打字的空文档，关窗再开要原样回来。

### 根因：会话的包含条件把它整条漏掉了

B68 把入会话判据从「只收有 `path` 的」放宽成 `!!d.path || d.backedUp`。
但**空的新建文档两个都不占**：

- 没有 `path`（未命名）；
- **不脏** —— 没输入过内容就不会触发 `scheduleBackup`，于是连副本都没有，
  `backedUp` 恒为 false。

结果：它压根写不进 `session.json`，下次启动自然无从恢复 →「新建了还没打字」的标签凭空消失。

### 修法：靠 `docId` 认领（而不是面板/标签索引）

给会话标签加 `TabSession.docId`（Rust `doc_id: Option<u64>`，= `doc::Doc::id`）：

1. **写入方向**（`snapshotSession`）：判据放宽为
   `有 path || 有副本 || (热退出开 && 无 path && 不脏)`，并把 `docId: d.tabId` 写进标签。
2. **恢复方向**（`restoreSession`）：`identityOf` 增加 `#doc:<docId>` 分支；
   预取时「既无 `path` 又无 `backupId`」= 空文档，调 `newTab` 造一个空白文档，
   放进 `restoredCache`（新增 `kind: "empty"`），且**不置脏**。

⚠️ **为什么必须是 `docId` 而不是「面板索引 + 标签序号」**：同一个空文档可以被分屏成
两个实例，按索引取两次就会恢复成**两份互不相干的文档**，破坏同源多实例 ——
和 B68 里「`restore_backup` 每次都新建标签」是同一类坑。按文档身份去重才对。

⚠️ **判定必须带 `!d.dirty`**：只写「无路径就收」，会把「**脏、但备份失败**」的未命名文档
也收进去，恢复时按空文档处理 → 真的把用户打的字丢掉。那种情况只能走关窗确认框
（快照里没有它 → 恢复时也不会被当成空文档）。

### 测试

- `tests/hot-exit.test.ts` +1（**写入方向**）：冷启动的空文档要进会话且带 `docId`；
  同时断言「没有内容可备份 → 不写副本」。
- `tests/hot-exit-empty.test.ts`（新文件，**恢复方向**，3 条）：会话里两个空标签（只有
  `docId` 7 / 8）→ 启动后回来**两个**空白标签（只造一个就说明第二个又丢了）、
  不读副本也不写副本、不亮 ●。
- `tests/regressions.test.ts` +4 静态契约：快照判据必须带 `!d.dirty`、会话 schema 与快照
  都要带 `docId`、`identityOf` 必须有 docId 分支且空文档走 `newTab`、恢复流程里
  只有副本分支能置脏。
- 顺带修正 B68 的一条守卫：入会话判据从一行 `return` 拆成两步，旧断言的正则对不上了。

**反向验证**（`git` 外临时脚本，用完还原并 diff 确认字节一致）：
① 去掉 `identityOf` 的 docId 分支 + 快照不收空文档 → 4 条用例精确变红
（恢复方向运行时、写入方向运行时、两条静态）；② 只去掉 `!d.dirty` → 对应静态守卫变红。

### 交付

- 规模：**388 vitest（+8）+ 34 cargo（+1）**。
- `npm run build:all` BUILD_EXIT=0；提交 `fix(hot-exit)` 见 git log。

### 环境/流程笔记

- `scripts/run-vitest.cjs` 自己会补 `run`，再手写 `run` 会变成
  `vitest run "run" "<file>"`（且失败时被吞掉 stdout）→ 排查时直接用 `npx vitest run <file>` 看输出。
- 会话 `TabSession` 标了 `#[serde(default, rename_all = "camelCase")]`，新增 `Option` 字段
  无需额外 `default`，旧会话缺字段自动回落 `None`。
