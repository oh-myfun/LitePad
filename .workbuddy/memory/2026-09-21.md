# 2026-09-21

## B88 收尾（保存时的脏写检查）— 已提交

- 提交 `59d4288` `feat(save): 保存时检测磁盘版本变化并弹框让用户选择（B88）`。
- ⚠️ 版本号那一步踩了老坑：`git checkout -- package.json …` **静默没落盘**（命令回显为空、
  退出码 0，但四个版本文件仍是 0.10.0）。最后用 Edit 逐个改回 0.9.0 才干净。
  → 再次印证「编辑回报成功 ≠ 已落盘」，**git 命令也算**，回退后必须 grep 复核。
- 版本号 bump 留给 `release.sh`（它要求工作树干净，所以 feature 提交里不能带版本改动）。

## B89 跨窗口拖拽：途中只预览，松手才提交（用户报的毛病）

用户原话：卫星窗口的标签/面板拖入拖出窗口时没有预览，一旦拖入或拖出就**立马生效**，
操作不连贯（想把窗口 A 的标签拖到窗口 B 的面板 A，经过面板 B 就被自动合入）。

### 根因
`splitview.ts` 的 `onTabDragMove` 一判定 `outsideWindow` 就 `finishTabDrag()` +
`onDragOutOfWindow(...)` —— **副作用发生在拖拽途中**，而不是松手时。于是：
· 主窗口拖出：鼠标还没松手就弹出新窗口；
· 卫星窗口拖出：指针擦过主窗口就整组合入，没法挑落到哪块面板。

### 改法
- 拖拽层（splitview）：出界只做三件事 —— 记 `tabDrag.outOfWindow`、清掉**本窗口**的落点
  痕迹、调 `onDragOutside`（通知宿主广播指针位置）。**不收尾、不搬运**。
  `onTabDragEnd` 见 `outOfWindow` 才 `onDropOutOfWindow`。
- 新增 `src/shell/windowdrag.ts`：跨窗口协议
  hover（广播坐标）→ 各窗口自己判定命中并渲染预览 → release（广播坐标）→
  命中的窗口 claim → 发起窗口 `emitTo` 定向发正文 → end 清场。
- 宿主（main.ts）：`dropTabsOutOfWindow` 先问「有没有别的窗口接手」（等 200ms），
  有 → 定向投递 + 本地摘标签（主窗口 `remoteTabLocally` 留隐藏实例 / 卫星 `detachLocally`）；
  没有（扔在桌面）→ 回落到旧语义（主窗口开新窗、卫星交回主窗口）。
- 影像贴边：出界后 ghost 若照坐标走会整个跑出客户区（看不见），改为 clamp 到边缘露一条。

### 三条不变量（写进 `ref/multiwindow.md` §9.4）
1. **Windows 隐式鼠标捕获**：鼠标按下后指针掠过别的窗口时，那个窗口**收不到任何鼠标
   事件**，发起窗口反而能继续收 mousemove/mouseup。所以「指针压在哪块面板上」只有目标
   窗口自己知道 → 必须走 IPC。
2. **正文绝不广播**：`emit` 是广播，每个窗口都收到一份完整 payload；标签快照带正文，
   几十 MB × 窗口数。所以 release 只带坐标，claim 之后才 `emitTo` 单独发。
3. **先挂 claim 监听、再广播 release**：反过来的话接手方的回答可能早于监听就位，
   这一拖只能干等超时再回落成「开新窗口」——表现为「明明拖到了另一个窗口却还是新建了」。
4. 坐标口径：一律**逻辑像素**（`outerPosition/outerSize/innerSize` 都是物理像素，
   必须除以 `scaleFactor`，高 DPI 屏上否则整体偏一倍）；几何一次拖拽问一次并缓存。

### 验证
- vitest **519 passed**（29 文件，+8 条 B89 用例：拖拽层行为 + 坐标换算纯函数）。
- 反向验证 `scripts/reverse-verify-B89.cjs` **14/14**（12 期望红 + 2 对照绿，sha256 还原一致）。
- 陈旧断言三处改写（不是删）：B64/B72 的 `moveDragGhost` 写法（加了 clamp 参数）、
  B71④ 的「出界即 finish+上报」→ B89 契约、`tabstrip-drag` 的「两种预览互斥」
  （落点判定抽成 `previewDropAt`，互斥做法改成「每次判定开头先清两种痕迹」）。
  ⚠️ `fnBody()` 只能定位 **main.ts** 的函数；splitview.ts 的函数要用 `topLevelFnBody(sv, …)`。

## 版本与打包（v0.10.0）

- 提交链：`59d4288`(feat save/B88) → `534efa6`(feat window/B89) → `b3e4cac`(docs memory)
  → `55b2831`(docs architecture) → `b035bc6`(chore release v0.10.0)，tag `v0.10.0` 已打。
- 本地全量构建通过：vitest 519 → cargo test 46 → `src-tauri/target/release/litepad.exe`（7.9M）
  → `bundle/nsis/LitePad_0.10.0_x64-setup.exe`（5.6M）。
- ⚠️ **推送失败**：`git push` 与 `curl https://github.com` 都是 21s 连接超时（网络不通，
  不是 09-19 那种 HTTP 408，重试无效）。等网络恢复后手动：
  `git push origin main --follow-tags`（pre-push 会再跑一遍本地构建门禁）。
- ⚠️ `release.sh` 无 PATH 兜底 → 见 `ref/session-env.md` §10（含「版本号已写入后失败」的补救）。

## B90 拖拽观感与空面板（用户二次反馈）

四条，全部落在 B89 刚改的那批代码上：

1. **影像始终精确跟随光标** —— B89 加的「出界贴边（clamp）」被否掉：指针在哪影像就得在哪。
   回退成原样（`moveDragGhost` 去掉第三个参数）。
2. **空标签栏的插入线不再顶到最右** —— `stripInsertInfo` 在无 `.tab` 时原先返回 `r.width`，
   指示线是 absolute 定位、一样参与滚动区域计算 → 拖过一块空面板就冒出横向滚动条。改返回 0。
3. **拖入又移出后预览残留** —— 之前只在「松手在窗外」收尾。现在 `finishTabDrag` 统一回调
   `onDragEnd`（松手 / 失焦 / 强制清场都走它）→ 宿主广播 END。
   ⚠️ 连带发现一个更隐蔽的顺序坑：发起窗口是「先 finishTabDrag（广播 END）→ 再提交落点 →
   投递正文」，所以接手方收到 END **只能清视觉、必须留住落点**（`clearReceiver(false)`），
   否则正文到达时已经不知道该放哪儿 → 预览在这、落下在那。
4. **移空的面板要摘掉** —— 新增 `pruneEmptyPanels()`：跨窗口拖走（主窗口 `remoteTabLocally` /
   卫星 `detachLocally`）、交回主窗口、开新窗口、以及**同面板分屏**把唯一标签挪走之后都调它。
   `moveTabToStrip`/`moveTabToPanel`/`moveGroupToPanel` 各写一遍判据，两条新路径漏了 → 收口。

验证：vitest **525 passed**（+6 条 B90）、反向验证 `reverse-verify-B90.cjs` **8/8**（6 红 + 2 绿对照）。

### ⚠️ 自己踩的坑：Edit 的 new_string 少写会静默删内容
改 `ref/multiwindow.md` 时 old_string 给了 3 行、new_string 只写了第 1 行 —— 结果后两行
**被静默删掉**（工具只报成功）。同一类事故的第三遍了（前两遍是 git 命令与门禁脚本）：
**多行的 old_string，new_string 必须包含等量的原文**，改完立刻 grep 回读。
