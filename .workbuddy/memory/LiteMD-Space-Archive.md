# 旧「LiteMD 空间」会话与记忆归档

> 归档时间：2026-09-13。目的：工作区目录由 `E:\Project\LiteMD` 更名为 `E:\Project\LitePad` 后，
> 把旧空间残留的会话与记忆**总结并迁入 LitePad 空间**，使 LitePad 空间成为唯一事实来源。

## 一、被归档的空间

| 项 | 值 |
| --- | --- |
| 空间目录 | `~/.workbuddy/projects/e-Project-LiteMD/` |
| 对应工作区 | `E:\Project\LiteMD`（已更名为 `E:\Project\LitePad`，目录不复存在） |
| 会话文件 | `2b4038af-c514-49e8-be76-7643793e8d7e.jsonl`（9439 行 / 约 36 MB） |
| 时间跨度 | 2026-09-10 23:25 → 2026-09-13 12:06 |
| 规模 | 178 轮用户输入；1321 条消息、2799 次工具调用、1363 条推理、1154 次文件快照 |
| 空间级记忆 | `~/.workbuddy/memory/2e9df769-*.md`（云端 profile 缓存，仍以 LiteMD 命名） |

**它就是本项目的全部开发史**：M0 脚手架 → M3 交付 → B4–B35 的缺陷修复与打磨，全部发生在这个会话里。

## 二、旧记忆内容摘要（归档时状态）

云端 profile 停在 **M3 开工前**的认知，具体为：用户是 LiteMD 项目开发者，做替代 Notepad++ 的
轻量 Markdown 编辑器（Tauri 2 + Rust + CodeMirror 6，Windows + MSYS2 MinGW，未用 VS Build Tools），
已交付 M0–M2，M3 是下一个里程碑；偏好简洁中文 + 技术细节 + 里程碑摘要；习惯发「继续」推进；
强依赖 Git、每次编译都要出 release；沙箱会拦截 WebView2 的 http(s) 加载，预览须走 `file://` 或 release build。

> ⚠️ **该 profile 已过期且未更名**：它既不含 M3 之后 B4–B36 的进展，也仍写着「LiteMD」。
> 因云端记忆由服务端托管（本地写入会被覆盖），此处仅登记事实，不对其做本地改写。
> 本项目的最新事实以本目录 `MEMORY.md` / `ARCHITECTURE.md` / `BUILD-ENV.md` 与逐日日志为准。

## 三、需求演进时间线（按用户原话归纳）

### 阶段 1｜M0–M3 主线交付

1. `@"LiteMD-技术方案.md" 开始开发` —— 依据技术方案开工。经确认的决策：
   **用已有 MSYS2 MinGW（不装 6–8 GB 的 VS Build Tools）、仅 Windows、本轮只做 M0**。
2. `继续` ×N —— 排查前端 bootstrap 未执行（会话限流中断后续做）。
3. 「可以正常打开，后续发布应用应该不用先启动 `npm run dev` 吧？」→ 确认 release 自带前端产物，无需 dev server。
4. **`注意用 git 进行维护管理，每次编译要把发布版本也编译了`** —— 从此确立 git + release 双要求。
5. `继续开发，直到M2完成` → M2 交付。

### 阶段 2｜UI 重构与分屏体系（B1–B8）

6. 「分屏、预览、源码的切换，打开文件的分屏布局，窗口关闭都有问题」
7. 「再打磨一下，确保分屏、标签、预览功能效果优秀」
8. **「markdown 只需要源码/预览切换，不要把分屏功能混到切换按钮里，分屏是独立的逻辑，
   无论怎么分屏，窗口都是被填满的。顶部按钮栏最前面无需标题，且最好有菜单栏（功能分类下拉）
   和工具栏（快捷图标按钮）」** —— 菜单栏 + 工具栏二分的由来。
9. 「只有一个文件时窗口没有填满，文件关闭逻辑也有问题」；「改进下菜单栏，工具栏及其图标」
10. 「关闭部分分屏区域时要关闭区域内文档」
11. 「关闭面板变成了新建文档，设置按钮图标怎么是一个太阳…关闭窗口确认不保存，还是没有关掉」
12. 「面板上的分屏按钮和工具栏的重复了，保留工具栏的。支持拖拽标签进行分屏
    （分屏前自动预览分屏结果，且拖拽到不同区域可进行不同分屏，就像 xshell 那样）」
13. 「打开的文档里面都没有内容，关闭窗口没有任何反应了」——拖拽分屏引入的回归。
14. 「深浅色按钮有时候要点两下才生效（深色切浅色时）」；「预览效果的行间距有点小，最好支持调节」
15. 「参考一下 win11 的 notepad 的布局，以及 markdown 预览切换按钮的设计（放在底部状态栏中）」
16. 「继续参考 notepad，进行布局优化，**你可以自己启动操作并截图进行参考**」——授权自取自证。
17. 「Plain Text 部分和 markdown 语法切换部分是同一个」
18. 「关闭窗口或者最后一个面板时报错，unhandledrejection: Command plugin:window|destroy not allowed by ACL」

### 阶段 3｜同源多实例与拖放架构（B9–B25）

19. 「菜单工具栏上无需显示文件名。支持 tab 标签拖拽分屏，另外当前点击分屏按钮和关闭分屏操作的逻辑
    有些混乱，先梳理下制定方案并让我选择」——要求先给方案再动手。
20. **「支持同一个文件复制到多个面板（比如左右分屏，左侧打开 markdown，可以在标签上右键复制一个标签，
    然后拖到另一个面板，方便一边看源码，一边看预览效果）同文件的多个标签页内容同步同源，内存里数据也是同一份」**
21. 「标签拖动有分屏预览，但没法真正分屏，且会在内容中添加 1，2 这样的数字」
22. 「预览时已经分屏区域再分屏的预览效果还不够准确」
23. **「我说过的每一个 bug，都要添加对应的测试用例」** —— 回归测试要求的由来。
24. 「深浅色切换，所有面板要一起跟着变」；「所有文件行号区域也要随深浅色变化，并优化折叠图标效果」
25. 「打开文件，内容全是空白的，但第二次打开恢复的窗口里对应文件却是打开好的」
26. 「同一个面板内文件标签切换失效」；「文件拖入窗口时应该打开该文件，而不是复制内容到当前窗口」
27. 「拖拽时指针光标改成更合适的，另外关闭部分面板时，其他面板分割位置会受到影响」
28. 「1. 更换面板点击 tab 标签，没法马上激活，要再点一下。2. 只有预览的 markdown 能看到滚动条，
    其他文本和源码都看不到滚动条，也没法滚动」
29. 「markdown 大纲颜色没有随深浅色变化（看下还有没有其他组件有同样的问题，一并修复）。
    大纲点击跳转没有对所有打开的同一个文件进行同步跳转位置」
30. 「json 等其他语言或格式是否也能支持折叠功能，添加折叠/展开全部的功能」
31. 「标题栏中软件名放前面，文件名放后面」；「应用图标有点丑，重新生成一个漂亮的」
32. 「release 版本打开时不要带命令行界面，当前 release 版本似乎依赖一个 dll，能将其打包到 exe 中吗」
33. 「图标下边缘改成和上边缘一样的圆角，且要应用到打包和执行文件的 exe 上」
34. 「大纲跳转预览状态的 markdown，位置不够准确」

### 阶段 4｜交互精修（B16–B33）

35. 「大纲区域也要支持宽度调节」；「滚动条样式和界面最好统一一下，并且也支持深浅色」
36. 「查找/替换功能，参考 notepad，用一个悬浮栏的形式实现，支持跨文件…目前查找功能有重复（两个按钮）」
37. 「帮助里面的设置窗口里的设置有点多余，将相关内容分别放到对应的菜单栏中」
38. 「支持 ctrl+鼠标滚轮进行缩放（调节字号），预览内容也要随缩放调节」
39. 「tab 区不显示滚动条，tab 数量太多超出面板宽度时，用更优雅的方式展示（自动折叠为一个下拉列表按钮），
    且 tab 区支持鼠标滚轮调节；左边和右边超出的标签都放入折叠下拉列表按钮」
40. 「光是切换 markdown 预览效果和源码，或者点击一个文件内容区，不应该改变文件内容」
41. 「大纲内容有时候没有随着当前选中文档进行切换」；「大纲区分割条样式和其他分割条保持一致」
42. 「源码模式时大纲跳转也应该是把对应位置跳转到页面顶部，而不是底部」；「『转到行』（Ctrl+G / F5）也顶部对齐」
43. 「查找/替换悬浮条无法关闭，再优化下布局和样式，且在预览模式下也要有和源码一样的效果（高亮和跳转）」
44. 「优化 tab 区折叠按钮样式，当折叠按钮显示的情况下打开关闭部分文件时的效果」
45. 「拖拽文件到窗口进行打开时，最好也显示分屏预览，并可拖入不同面板或分屏区域进行打开，
    拖住 markdown 文件时，弹出菜单让用户选择插入文件路径或者打开文档」
46. 「打开文档或关闭文档时，面板间的分割条位置不要变」；「拖拽 tab 时，如果在 tab 区，则是调整 tab 顺序，
    预览效果和分屏的预览效果要区分开」
47. 「当前支持大纲的格式有限，toml/json 等还不支持，尽量多支持一些格式。并且当前激活文件从带大纲文件
    切换到不带大纲的文件时，左侧大纲区域没有清除」

### 阶段 5｜更名与工程化（B34–B36）

48. **「应用名改为 LitePad，重新生成和替换图标，要求图标背景四个角都为圆角，图标样式简洁美观，
    贴合应用名和功能」** —— B34（当时只改了 11 个源码文件）。
49. **「完善 git 发布流程和本地格式检查流程，为后续推送到 github 做准备」** —— B35。
50. **「梳理项目中所有文件，将其中的 LiteMD 改成 LitePad，并提交修改，推送到 Github。
    将 WorkBuddy 中所有 LiteMD 空间的会话和记忆内容总结并挪到 LitePad 空间」** —— B36，即本次。

## 四、沉淀下来的用户偏好与决策

- 表达偏好：**简洁中文 + 技术细节 + 清晰的里程碑/条目摘要**；喜欢「Bxx」式缺陷编号追踪。
- 节奏：逐个里程碑顺序推进，常用「继续」驱动；大改动要求**先给方案再选择**。
- 硬性要求（已固化进 `MEMORY.md` 项目约定）：每个交付一个 git 提交；每次编译都出 release；
  每个 bug 必配回归测试；改名前要连注释一起清。
- UI 基准：以 **Win11 记事本**为参照（布局、菜单、查找替换、状态栏视图切换）。
- 沙箱认知：清楚会话内无法做 UI 冒烟，UI 效果由自己桌面验证。

## 五、迁移结论

- 本项目 M0–B36 的全部事实，已由本目录 `MEMORY.md`（索引/约定）、`ARCHITECTURE.md`（不变量）、
  `BUILD-ENV.md`（环境命令）、`2026-09-*.md`（逐日细节）承接，**不再依赖旧空间的原始 jsonl**。
- 旧空间 `~/.workbuddy/projects/e-Project-LiteMD/` 中的会话文件为**未摘要的原始日志**（36 MB），
  内容已被上述文档覆盖。
