# 2026-09-22

## 清理 src-tauri/target/debug（用户指令，非里程碑开发）

- 用户要求清掉 `src-tauri/target/debug`（~11GB 编译产物，gitignore），保留 release。
- 已执行：`rm -rf src-tauri/target/debug` → `target/` 13GB → **1.7GB**（仅 `release/` + `CACHEDIR.TAG`）。
- 用户偏好：**后续尽量只编译 release**，避免 debug 体积。
- 复查构建脚本，发现以下命令默认仍走 debug，**会再生 ~11GB debug**：
  - `scripts/build-all.sh:37`：`(cd src-tauri && cargo build && cargo test)`（无 `--release`）
  - `scripts/dev.sh`：`exec npm run tauri dev`（`tauri dev` 设计上必为 debug，无 `--release` 一说）
  - `.githooks/pre-push:90`：`(cd src-tauri && cargo test)`（无 `--release`）
  - 走 release 的：`build.sh`/`release.sh` 的 `npm run tauri build`、`pre-push:157` 的 `tauri build`、`build-all.sh:41` 的 `tauri build`。
- **已落地**（用户确认「其他命令也不要再生 debug」）：
  - `build-all.sh:37` → `cargo build --release && cargo test --release`（步骤 3 注释同步改 release）；
  - `.githooks/pre-push:90` → `cargo test --release`（echo 文案同步）；
  - `build-all.sh:45` 的 `ls …/target/debug/litepad.exe` 已删除（该 exe 不再存在，release 清单已由下一行覆盖）。
  - 复查：`scripts/` 与 `.githooks/` 内**再无任何 `cargo build/test` 不带 `--release`**（grep 全量确认）；
    仅剩的 debug 生成源是 `dev.sh` 的 `tauri dev`（设计上必为 debug，无法改；开发热重载时例外）。

## 顺带：InkNote 研读提案的后续（见 09-19 日志 + open-items/inknote-benchmark.md）

- 09-19 起的提案仍处待确认状态，本次未推进。S2（测试纳入类型检查，实测 52 错误）仍是最优先低风险项。
