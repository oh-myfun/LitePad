# LitePad 未决事项与遗留清单（按需读取）

> 本文件**不**在每次会话自动加载（只有 `MEMORY.md` 会被整篇注入）。需要回顾「还有什么没做 /
> 哪些待真机确认」时再读它。
> 已交付细节见 `ARCHITECTURE.md` 与各 `2026-09-*.md` 当日日志。
> 本清单由 `MEMORY.md` 的「进度」与「下一步」迁出（09-18 重构，给索引瘦身）。

## 已交付里程碑速查（不变量见 `ARCHITECTURE.md`）
- M0–M4：脚手架 → 多标签/搜索/设置/日志 → 分屏/会话恢复/自动保存/文件监听/跨文件搜索 →
  Markdown 渲染 → 大文件分级降级 + 命令面板 + 三档键位预设（default / notepadpp / vscode）。
- B34–B68：更名+图标 / prettier+eslint+CI / 标签栏**原生横向滚动**（折叠机制已删）/ 首选项弹窗 /
  启动白屏 / 安装包与安装器图标三套机制 / 分屏对齐 VS Code(B59–B63) / B64 拖拽影像 /
  **B65+B66 ●↔× 互斥** / B67 双击按分割数均分（行为早有，文档没跟上）/ **B68 热退出**（Hot Exit；
  顺带修 M4 大文件降级从未生效——`OpenedFile.size_class` 按 snake_case 读）。
- B70 提示层三档、B71 面板操作对齐 VS Code + ④ 完整多窗口、B72 拖拽影像药丸 + 卫星窗口建窗修复。
- **不变量索引**：保存体系 §7 / 多窗口 §9 / 菜单 vs 面板·提示互斥 §4·§7 / tooltip 自绘层 §4。

## 待真机确认（沙箱起不了 WebView2，全部无力自证）
- ⚠️ **B72**：① 拖出标签/整组时**新窗口真的打开**（开窗后核对 `smoke_log` 里 `browser args`
  与主窗口一致）；② 整组拖拽跟随光标的**药丸**观感（圆角/字号/长文件名截断且 `(+N)` 仍在/光标不压字）。
- ⚠️ **B70**：① 菜单开着划过工具栏/状态栏**不弹**提示，关菜单后恢复；② 拖非 md 到 md 面板弹选择菜单、
  拖 md 到 txt 面板**不弹**直接打开；③ 命令面板鼠标上下挪行列表**不**弹回顶端；开菜单按 Ctrl+Shift+P 菜单消失。
- ⚠️ **B71 ④**：拖出落点（含多显示器）/ 卫星窗口拖回主窗口 / **两窗口同开一份文档正文实时同步** /
  强杀卫星窗口后主窗口接管隐藏实例 / 卫星窗口未保存内容的热退出恢复 / 最大化窗口贴边拖动不误弹新窗。
- ⚠️ **B68**：① 有未保存修改关窗**不弹**确认框；② 重启后未保存标签（含草稿）原样回来且仍脏；
  ③ 保存后副本被丢弃、重启**不回滚**；④ 关掉「文件→热退出」恢复弹框并清空现存副本。
- ⚠️ **B60–B66**：1px 静息线 / 4px 激活线 / 角手柄 `all-scroll` 手感 / 深色下浅蓝落点；联动在 2×2 与
  3 栏各验；B64 影像跟手；B66 关闭区 20px 手感与 `transition: opacity` 是否迟钝。

## 待补截图（沿用 B60–B66 欠账，沙箱拍不了）
- M4 `keymap.png`/`command-palette.png`；B51 `main.png`/`preferences.png`；B53–B71 `main.png`
  （标签图标 / 面板操作栏 / 分隔条细线 / 分屏落点 / ●↔× 换装 / 最大化后的「还原」按钮）。

## 待清理
- `menu.ts` 的 `MenuItem.active` 与 `.menu-item-current`（B53 后无使用者）。

## 待定
- 是否发 **v0.3.1**；M5 规划未定（候选见 `DESIGN.md`）。
- **应用自身日志** `%TEMP%\litepad-app.log` / `litepad-smoke.log`（Rust 侧 `commands/mod.rs` 经
  `std::env::temp_dir()`）是否也收进项目内或 `%APPDATA%\LitePad`（`MEMORY.md` 的「临时文件落项目内」
  规则未覆盖此项，属产品行为）。
- `%TEMP%` 历史残留约 50MB（`litepad-*-profile` / `*-sim` / `notepad_ref` 等）待用户确认后清理。
