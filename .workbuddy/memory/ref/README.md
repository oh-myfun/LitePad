# ref/ — 领域参考索引

> 每个文件是一个子系统的「改代码前必须知道」不变量。按需读取，不要在会话开头整篇加载。
> 路由总表见 `MEMORY.md`。踩坑根因见同级 `pitfalls/`；约束目录见 `rules/index.md`。

| 域 | 文件 | 说明 |
|---|---|---|
| 文档/实例模型、布局、拖拽、视图红线、样式、功能落点、通用教训 | `architecture-detail.md` | 由旧 `ARCHITECTURE.md` §1–§8 迁出（深层不变量） |
| 多窗口（卫星窗口） | `multiwindow.md` | 卫星窗口同步/拖出拖回不变量（原 §9） |
| 沙箱/构建环境特有坑 | `session-env.md` | **内部**：PATH/WebView2/进程清理等，不进 `docs/` |

> 与 `docs/architecture.md`（可发布总览）配套：总览给「一句话」，本目录给「为什么 + 判据」。
> 改语义须两处同步。新增/大修某域时顺手把不变量补进对应文件。
