# 2026-09-22

## 交付流程收紧：完成功能 = 自动「提交 + 打包」，不推送（用户指示）

- 09-21 的说法是「不要每次修改都推送」，我收尾时理解为「等你发话再推」，于是连打包也没做。
  用户 09-22 明确：
  > 「完成功能后自动不推送，但是要提交和打包」
- 新流程（已写进 `MEMORY.md` 红线区）：**commit → 本地打包 → 停手**三件自动做完，
  **推送仍需用户显式要求**。本地打包 = `scripts/build-all.sh` 四步，**不 bump 版本、不打 tag**
  （版本号只在 `release.sh` 里动，达阈值才 `npm run release patch/minor`）。
- 因此在 B91-2 三处回归已提交（`ca983d2`/`eb6600d`/`59f6954`）之后补跑了本地全量打包。

## 本地打包产物（v0.11.0，未 bump、未打 tag、未推送）

- `src-tauri/target/release/litepad.exe` 7.9 MB
- `src-tauri/target/release/bundle/nsis/LitePad_0.11.0_x64-setup.exe` 5.6 MB
- 四步全绿：tsc → vite build → vitest 555 → cargo build+test 46 → tauri release（2m41s）+ NSIS，
  全程 5m04s。
- 版本号仍 `0.11.0`：距 tag 9 个提交，其中「有效」（feat/fix/perf/refactor/revert）只有 2 个，
  远未达 patch 阈值 10（`scripts/check-version-bump.sh` 的阈值看的是**有效提交**，不是总提交）。
- 未刷 `docs/screenshots/`：本次交付是行为修复（拖放拦截），不影响观感。

## 沙箱新坑：`bash` / `npm` 会撞 wsl 黑名单（记进 `ref/session-env.md` §11）

- 症状：`bash -c 'echo hi'` 返回乱码 + exit 1，报 `PROGRAM BLOCKED … wsl.exe`；
  `npm run <script>` 秒级失败，日志只有 UTF-16 的「拒绝访问。」（Read/Grep 都识别成二进制）。
- 根因：PATH 里 `/c/Windows/system32` 排在 Git 之前，而 Windows 自带
  `C:\Windows\System32\bash.exe` **就是 WSL 启动器**；`npm` 在 Git Bash 下是 bash 脚本，
  shebang `#!/usr/bin/env bash` → 跟着走 wsl。安全策略不可绕过。
- 处置：跑脚本用 Git bash 绝对路径 `"/d/Program Files/Git/bin/bash.exe"`；node 工具全部
  **直调 node_modules 入口**（tsc / vite / eslint / prettier / @tauri-apps/cli），cargo 用
  `/c/Users/maoyu/.cargo/bin/cargo.exe`。完整配方见 `ref/session-env.md` §11。
- 附带：Bash 工具的 PATH 只含 Git 的 `cmd/` 不含 `usr/bin`，故 `ls`/`grep`/`cp`/`tail` 全缺失 ——
  文件操作改 PowerShell（`Copy-Item`），看日志用 Grep/Read，别 `| tail`（会截断，见 `pitfalls/0095`）。
- 打包脚本当时落在 `.tmp/build-local.sh`（`.tmp/` 已 gitignore，不入库）；配方已入库到
  `ref/session-env.md`，下次照方重建即可，不要去改 `scripts/build-all.sh`（它用 npm，对用户是对的）。

## 反向验证搬进 tests/（用户指示）

- 用户：「反向验证也是一种测试用例，可以放到测试目录」—— 对。此前反向验证只是
  `scripts/reverse-verify-*.cjs`（改真实源码 + 子进程跑用例 + 按字节还原），代价是被硬杀
  会留补丁（`pitfalls/0095`）、`-t` 过滤器写错会假绿。
- 新增 `tests/reverse-verify.test.ts`（提交 `13b4e29`，6 条）：用**退化实现替身**完成同一件事 ——
  把错误写法复刻成可安装处理器并断言它确实坏掉，与真实实现的基准用例对照；两者结果不同
  ⇒ 正向用例不是恒真 ⇒ 守卫咬得住。不改源码、无子进程、随 `npm test` 一起跑。
- 覆盖两组：文件拖入（0096：捕获阶段 / stopPropagation / preventDefault 缺一不可，三条退化
  各自证明一半）、标签拖拽（0094：只放私有 MIME vs 退化写 `text/plain`）。
- ⚠️ 关键细节：退化用例先断言 `defaultPrevented` **自证处理器确实跑了**，否则「编辑器收到」
  可能只是因为根本没人拦 → 退化用例恒真（同「退出码 0 ≠ 生效」那类假象）。
- 反向验证：把 `filedrop.ts` 的 CAPTURE 改回 `{ capture: false }` → 「基准」用例变红，恢复后 6/6 绿。
- 边界（写进文件头）：替身证明不了「源码真的没这么写」—— 那一半仍靠各域静态契约
  （`filedrop.test.ts` / `tabdnd.test.ts`）；`scripts/` 那层保留作补充。
- 门禁：tsc / eslint / prettier / vitest **561**（+6）全绿。已同步 `MEMORY.md` 红线与
  `docs/conventions.md`「测试与验证」。

## 测试整理：拆掉 regressions.test.ts + 退役反向验证脚本（用户指示）

- 目标：把 `tests/regressions.test.ts`（拆前 ~4050 行 / 159 条 `it`）按模块拆到各域测试文件，
  并把 `scripts/` 的 12 个 `reverse-verify-*.cjs` 迁移/退役。分三批提交（每批全量门 + 提交）：
  `568427e`（抽 static.ts）→ `1210f04`（第一批）→ `be60fd7`（第二批）→ `8b1a1b6`（第三批，
  regressions.test.ts 删除）→ 本批（反向验证搬移，`576` passed / 42 files）。
- 共享静态断言 helper 抽到 `tests/static.ts`（themeBlock / ruleBlock / cssDecls /
  stripLineComments / topLevelFnBody / FILE_FAMILIES / collectTextFiles 等）；
  所有「防假绿注释」一并迁过去。
- ⚠️ **拆出来的用例漏 import 不会在 tsc 阶段暴露**—— 见下条 tsc-include 缺口坑。
  补 import 后 562 条用例标题与拆前基线**逐条比对零丢失、零新增**。

## ⚠️ 新坑：tests/ 不进 tsc，缺 import 只到运行时才红（pitfalls/0097）

- `tsconfig.json` 的 `include` 只有 `["src"]` → `tsc --noEmit`（pre-commit 的 `tsc -b`）**完全不查 tests/**。
  后果：把用例从 regressions 搬到新文件时漏了 `import { themeBlock } from "./static"` 之类，
  类型阶段全绿，但 vitest 一跑就是 `ReferenceError: themeBlock is not defined`（本批一次红了 35 条）。
- 处置：写 `.tmp/check-imports.cjs` 静态扫「用到但没 import」的标识符（覆盖 static.ts 导出名 +
  node:fs / node:path 绑定）；补完再跑全量。注意它会有误报（数组 `.join(`、`.then`、`.Promise.resolve`、
  以及 `await import("node:fs")` 动态导入），需人工 glance。
- 长期修复候选（暂缓）：在根 tsconfig 之外加 `tsconfig.tests.json`（`include:["tests","src"]`，
  配 `types:["node","vitest"]`，但缺 `@types/node`，需装）并把 pre-commit/CI 改成 `tsc -b` 双工程；
  或 Keep 现状但把「tests 静态 import 自检」加进 `scripts/run-vitest.cjs` 前跑。先记坑，待定。

## 反向验证脚本退役（09-22 本批）

- 删 `scripts/reverse-verify-{B77,B78,B79,B80,B88,B89,B90,B91,b70,b72,b73,b76}.cjs`（12 个）。
  根因：它们**全硬编码**了 `tests/regressions.test.ts` 当 vitest 目标；该文件拆掉退役后，脚本因
  「目标不存在」而 vitest 退出非零，`expectRed` 探针一律报「红 ✓」= **静默假绿**，比不跑还危险。
- 分流（按「只迁未被覆盖的，其余退役」）：
  · 能退化替身表达、且之前无守卫的 → 搬进 tests/：新增 `tests/static-guards.test.ts`
    （反向验证 static.ts 守卫基建自身，来源 B77 甲组 / pitfalls/0075）；`tests/reverse-verify.test.ts`
    补 restrictToRange / preserveCase 退化对照（来源 b73）。
  · 其余（CSS 外观数值、Rust wire）→ 正向契约已在各域静态契约用例里，脚本一次性使命完成，退役。
- 顺手删掉一个**真·重复**：「版本号四处同步」在 `packaging.test.ts` 与 `repo-hygiene.test.ts`
  各有一份 → 删 `repo-hygiene` 那份（标题完全相同）。
- `docs/conventions.md` 反向验证段落的 `tests/regressions.test.ts` 改成「对应模块的静态契约用例」。
- 全量 576 passed（42 files），prettier / eslint / 钩子全绿。
