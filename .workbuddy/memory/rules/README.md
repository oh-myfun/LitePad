# rules/ — 约束与红线目录

> 本目录是项目约束的**分类索引**。`index.md` 按「红线 / 文档模型 / 布局拖拽 / 视图样式 / 构建环境 / 范围」
> 分类列出每条约束，并指向 `ref/` 深层不变量或 `pitfalls/` 踩坑根因。
> 最高优先级的「红线」由 `MEMORY.md` 内联；这里是**全量分类版**。
> 路由总表见 `MEMORY.md`。
