node_modules/
dist/
target/
.DS_Store
*.log
.env
.env.*
!.env.example
.vite/
src-tauri/gen/
tsconfig.tsbuildinfo
generated-images/

# 项目内临时文件统一落点：日志、探针/一次性脚本、CDP profile、对照副本、临时下载。
# ⚠️ 临时文件一律写这里，**不要写全局目录**（`%TEMP%`、`/tmp`、`~/.workbuddy/`）——
#    规则见 .workbuddy/memory/MEMORY.md「临时文件一律落在本项目内」。
.tmp/

# VS Code 参考源码（第三方 MIT 只读副本）—— 整个目录不入库。
# 含上游 src/、codicons/、file-icons/、theme-icons/、product/ 等批量副本，
# 以及顶层元数据 INDEX.md / LICENSE.txt / REVISION*.txt。
# 全部可由 scripts/ 下的抓取脚本按脚本内**钉死的版本**精确复现（REVISION*.txt 是脚本产出的记录，非输入）；
# 真正入库的是抓取脚本本身（scripts/）与其生成物 src/shell/codicons.ts。
docs/vscode-reference/

# Python 截图 / 探针脚本产物（scripts/capture-screenshots.py 等运行期生成，不入库）
__pycache__/
*.pyc
.pytest_cache/

# 工具链崩溃转储（cargo / MinGW 偶发）
*.stackdump

